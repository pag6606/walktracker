---
scope: App iOS nativa SwiftUI que reemplaza el stack Capacitor/WebView; dominio portado desde domain.js/motivation.js validados en producción; 18 CAPs
purpose: build-substrate
altitude: feature
updated: 2026-09-12T10:00
---

- (decision) [ADOPTED] Sustrato = SwiftUI puro (opción B). Decidido por Paul 2026-09-12 tras costear 3 opciones en OPCIONES-SUSTRATO.md: A Capacitor+rediseño ≈5,5 fds, B SwiftUI ≈5,5 fds, C Flutter consolidar ≈4,0 fds. Criterio de Paul: 'nativo' significa literalmente Apple. Binds: todo. Prevents: que el sustrato se decida por mockup en vez de por ADR.
- (constraint) Heredado del SPEC, no re-decidir: arquitectura hexagonal (dominio puro sin frameworks ni UI, puertos en el dominio, adapters en el borde); no-backend; usuario único; dominio preservado (cronómetro wall-clock, pausa explícita única, sesión finalizada inmutable, zancada congelada al cierre, validación en la frontera, cadencia solo sobre tramos medidos, pasos estimados siempre desglosados y marcados); licencias Apache-2.0/MIT; targets ≥44pt; claro/oscuro; UI en español.
- (decision) Dominio en Swift IDIOMÁTICO, no transliteración de domain.js (decisión de Paul: 'una decisión de arquitectura, no código portado por un jr'). Value types, métodos, throws/Result, nombres del lenguaje ubicuo de domain-model.md §1. Binds: capa domain. Prevents: Swift feo e inmantenible que nadie revisa, y la falsa seguridad de creer que parecerse a domain.js equivale a comportarse como domain.js.
- (decision) El catálogo de 14 logros y las constantes de las fórmulas salen del código a DATOS versionados en el bundle, leídos y validados al arrancar. Binds: CAP-8, AchievementEngine, GoalEngine. Prevents: el incidente Dart — 7 logros con semántica alterada por transcripción manual. El catálogo deja de ser algo que alguien pueda teclear mal.
- (question) Consecuencia no resuelta de elegir Swift idiomático: las 168 aserciones JS ya NO se pueden portar 1:1 (las firmas cambian). O se portan como vectores dorados neutrales de lenguaje (datos, no código) que ejecutan las dos implementaciones, o se reescriben a mano en Swift y se pierde la equivalencia demostrable. Pendiente de Paul.
- (decision) Resuelta la pregunta abierta de las 168 aserciones: se extraen a VECTORES DORADOS en datos neutrales de lenguaje (entrada → salida esperada), no se portan como código Swift. Mismo mecanismo que el catálogo: la verdad vive en datos, no en ninguna de las dos implementaciones. domain.js y el dominio Swift ejecutan el MISMO fichero de vectores. Binds: dominio, CAP-1/3/4/7/8. Prevents: que 'idiomático' se convierta en coartada para cambiar comportamiento sin que nadie lo note.
- (version) Verificado en web 2026-09-12: Swift 6.3.2 estable (jun 2026), Swift 6.4 en el ciclo WWDC26; Xcode 26.6 estable (25 jun 2026), Xcode 27 beta 6 (24 ago 2026). iOS 26.6.x actual; iOS 27 sale el 14 sep 2026 (en 2 días). iOS 27 soporta iPhone 11 y posteriores.
- (direction) Hallazgo que reencuadra los mockups: desde iOS 26 el lenguaje visual de Apple es Liquid Glass. Fuente Apple/WWDC25-26: una app que usa controles SwiftUI nativos ADOPTA Liquid Glass automáticamente con solo recompilar contra el SDK. Los mockups v3 están dibujados en estética iOS 17 plana — dos generaciones de diseño atrás. Si 'nativo' significa literalmente Apple, gran parte del rediseño no se dibuja: se hereda del framework.
- (assumption) [ASSUMPTION] Dispositivo de Paul: UDID 00008110-001C318E0CF8401E → prefijo de SoC A15, y el memlog de la party registra que NO tiene Dynamic Island → iPhone 13/13 mini/14/14 Plus o SE3. Todos soportan iOS 26 y 27. Pendiente confirmar con Paul la versión de iOS que corre hoy.
- (decision) [ADOPTED] Deployment target iOS 26.0. Dato de Paul: iPhone 14 con iOS 26. Deroga el 16.1 del ciclo Capacitor (piso ActivityKit para WebView). Binds: todo el proyecto. Prevents: pagar 'if #available' hacia iOS 16/17 para un universo de instalación de un solo dispositivo. Habilita sin condicionales: Liquid Glass por SDK, @Observable/Observation, SwiftData maduro, ActivityKit actual. No se pone en 27 (sale el 14 sep, Paul está en 26): el suelo no puede exigirle actualizar para correr su propia app.
- (constraint) Hardware de validación confirmado: iPhone 14 (A15, SIN Dynamic Island). CAP-18 se valida solo en el layout de pantalla de bloqueo; la Dynamic Island queda 'que compile, sin hardware para validar' — coherente con lo ya acordado en la party de epics.
- (decision) Escritor único: SessionStore @Observable @MainActor es el ÚNICO que muta la sesión viva. CMPedometer, restauración de foreground y Live Activity NO escriben: entregan eventos y saltan al main actor. Binds: CAP-1/2/3/4/18. Prevents: dos copias del estado sincronizándose (el riesgo del actor + proyección) y escrituras concurrentes sobre un agregado que el SPEC exige inmutable al cerrarse. Descartado: actor de sesión fuera del main — cambia un riesgo de concurrencia inexistente a este volumen por un riesgo de coherencia real.
- (decision) Reconstrucción de gap (CAP-3) es ATÓMICA (decisión de Paul): mientras reconcilia, la sesión no acepta comandos. Binds: CAP-3, CAP-1. Prevents: cerrar una sesión con pasos que llegan tarde, y que finish() compita con una query histórica en vuelo.
- (decision) 'reconciling' es estado del SessionStore (capa de aplicación), NO del agregado Session. SessionStatus se queda en idle/active/paused/finished. Binds: dominio, domain-model.md §2. Prevents: que una necesidad de UI/concurrencia contamine el agregado y rompa los vectores dorados y el contrato de dominio heredado del SPEC.
- (question) Abierto por la atomicidad: la reconciliación debe estar ACOTADA en tiempo — si la query al coprocesador se cuelga, la UI queda bloqueada sin salida. Falta decidir timeout, qué se hace al agotarse (¿degradar a estimación marcada ~? ¿abortar y reintentar?) y qué feedback ve el usuario durante la espera. Resolver antes de finalizar.
- (decision) [ADOPTED por Paul] Persistencia = ficheros JSON Codable en Application Support, escritura atómica (temp + rename). Descartados SwiftData (obliga a dos modelos —@Model del framework + value types del dominio— y una capa de mapeo, para 1.500 filas) y GRDB (dependencia de terceros innecesaria). Volumen real: ~12 sesiones/mes → ~1.500 en 10 años, cabe en memoria. Binds: CAP-9, CAP-14, CAP-15, CAP-10. Prevents: acoplar el dominio al framework de persistencia y abrir un segundo sitio donde el modelo puede divergir.
- (decision) Un fichero por preocupación: sessions.json, achievements.json, settings.json. Cada uno con campo schemaVersion. Prevents: que guardar un ajuste reescriba el histórico completo (radio de daño) y que una corrupción se lleve todo.
- (decision) El formato en disco ES el formato de intercambio de CAP-14. Export/import no tiene serializador propio. Prevents: dos serializaciones del mismo dato que divergen en silencio.
- (decision) Concurrencia: Swift 6 strict concurrency completa. Todos los tipos del dominio son value types Sendable. Prevents: que el aislamiento quede implícito con cuatro caminos de escritura.
- (decision) Sesión activa = presentación modal (fullScreenCover) sobre el TabView de 4 pestañas, no una quinta pestaña. Binds: CAP-1, navegación. Prevents: que la sesión sea navegable/abandonable como un destino más; una sesión es un modo, no un sitio.
- (decision) Liquid Glass se hereda: se usan controles SwiftUI nativos y NO se dibujan materiales a mano. El dibujo propio se limita a tres piezas: anillo de meta, gráfico de tendencia e insignias. Binds: toda la UI. Prevents: recrear a mano la estética iOS 17 plana de los mockups peleando contra el SDK de iOS 26.
- (decision) Textos de UI en String Catalog (.xcstrings), no literales dispersos. Binds: UI, constraint 'celebrar nunca culpar'. Prevents: que el tono se pierda cuando el copy vive en 30 vistas.
- (constraint) Envoltura operativa: dev = build local Xcode sobre iPhone 14 físico (el simulador no tiene coprocesador de movimiento real); distribución = TestFlight opcional, no requisito de éxito; sin CI; sin crash reporting ni telemetría de terceros (constraint de Privacidad del SPEC lo prohíbe).
- (assumption) [ASSUMPTION] Live Activity (CAP-18) exige un Widget Extension como target aparte, y por tanto un App Group para compartir estado entre app y extensión. Es estructura, no invariante, pero condiciona el árbol del proyecto desde el primer commit.
- (version) Verificado en web 2026-09-12: Swift Testing es el default recomendado para tests nuevos en 2026 (macros, paralelo por defecto, async nativo); XCTest se queda para UI tests (XCUITest) y performance (XCTMetric). Se adopta Swift Testing para dominio y aplicación; XCUITest diferido.
- (event) Reviewer Gate ejecutado: lint determinista 0 hallazgos + 6 lentes en paralelo (reconcile-spec, reconcile-domain-js, reconcile-validacion, rubric, vigencia-tecnologica, adversario). Resultado: CAMBIOS REQUERIDOS. ~40 hallazgos, 14 pares de divergencia construidos por el adversario.
- (decision) AD-6 REESCRITO tras el gate. El original era inviable: (a) 168 era un número reteclado sin verificar — son 281 ejecutadas; (b) solo 65 (36%) son puras entrada→salida, 111 son secuencias sobre agregado mutable y 52 prueban la agregada v1 de vueltas que domain-model §2 elimina; (c) NINGUNO de los 4 ficheros contiene aserciones de GoalEngine ni AchievementEngine, o sea que los vectores no habrían detectado el incidente Dart que AD-5 dice prevenir (first_5km es uno de los 8 logros sin cobertura); (d) congelaba como contrato los bugs que domain-model §9 ordena corregir. Nuevo diseño: 3 categorías (vectores puros extraídos / escenarios Swift portados a mano / exclusiones declaradas) + una lista de DIVERGENCIAS DECLARADAS donde Swift debe fallar contra domain.js a propósito.
- (decision) Bugs verificados en producción (v3) que el port debe corregir, no replicar: domain.js:349-352 resta las pausas dos veces (cadencia 84,4 vs 80,3 correcto; ritmo 1085 vs 1140); motivation.js:156-159 ordena fechas lexicográficamente sin zero-padding y rompe rachas al cambiar de mes; domain.js:249 y :392 llaman Date.now() dentro del dominio violando el ClockPort.
- (decision) Cinco AD nuevos del adversario adoptados: AD-16 un fichero un dueño; AD-17 disparador único de logros + desbloqueo irrevocable; AD-18 arco de sesión huérfana; AD-19 un solo AppCalendar (timeZone + firstWeekday, hora local); AD-20 superficies de comando y acciones irreversibles. Más AD-21 presupuesto de energía, AD-22 sin fuente no se pinta, AD-23 higiene de repo, AD-24 atribución Open-Meteo.
- (version) Correcciones de vigencia: Swift 6.3.3 (no 6.3.2) — es lo que trae Xcode 26.6. CMPedometerData NO es Sendable (ni sus NSNumber): exige DTO propio en el handler. queryPedometerData tiene ventana de 7 días y falla en SILENCIO con datos parciales. ActivityKit NO requiere App Group (ContentState viaja por request/update, tope 4 KB); sí requiere NSSupportsLiveActivities en Info.plist. Open-Meteo es CC-BY 4.0: exige atribución visible. HKWorkoutBuilder vigente y Sendable.
- (question) Riesgo de calendario: iOS 27 sale el 14 sep y el iPhone 14 de validación se actualizará. No hay fuente que confirme que Xcode 26.6 pueda depurar en un dispositivo con iOS 27, y el simulador no sirve para CAP-2/CAP-3. Decisión de Paul pendiente: retrasar la actualización del iPhone, o subir a Xcode 27.
- (event) spine finalized — 24 ADs, lint limpio, companions DEROGACIONES.md y REESTIMACION-EPICS.md escritos. Reestimación 5,5 → 7,25 fines de semana.
- (decision) [ADOPTED] OQ-5 resuelta por Paul: dispositivo congelado en iOS 26, actualizacion a iOS 27 en standby hasta fin de desarrollo. Toolchain fijo Xcode 26.6 / Swift 6.3.3. Refuerza AD-2: el suelo iOS 26.0 deja de ser una eleccion de conveniencia y pasa a ser tambien el techo de validacion durante el ciclo
- (decision) Paul ordenó arreglar los bugs de la v3 ANTES de portar, contra el argumento de eficiencia de Amelia ('se arreglan solos al portar'). Resultó ser mejor arquitectura: arreglar la referencia antes del port hace converger domain.js y el dominio Swift en vez de divergir, reduce la tabla de divergencias declaradas de AD-6 de 4 filas a 2, y permite validar el arnés de vectores contra un runtime existente antes de apostarle el port. Corregidos: doble resta de pausas (3 sitios, finishV3 ahora reutiliza calculateCadence) y checkStreak lexicográfico. 438 aserciones en verde.
