---
scope: WalkTracker iOS (Capacitor) — app instalable que reutiliza la PWA v3 en WebView con plugins nativos: CMPedometer background, HealthKit, keep-awake, local-notifications, Live Activity
purpose: build-substrate
altitude: feature
updated: 2026-07-28T23:06
---

- (note) Input: spec package _bmad-output/specs/spec-walktracker-ios (SPEC.md + companions + memlog, 17 CAPs, 0 OQs). Hereda silenciosamente: Capacitor (OQ-1), hexagonal, dominio preservado, no-backend, usuario unico, licencias MIT/Apache. Referencias vivas: PLAN-CAPACITOR-v2.md (draft v1.1, 4 ADs + 5 stories) y ARCHITECTURE-SPINE-v3 (PWA)
- (direction) Paul elige Coaching path; entregable = solo el spine (sin walkthrough ni docs extra)
- (decision) DECISION 1 (Paul): frontera nativa consolidada en UN plugin custom (semilla WalkTrackerKit) que envuelve CMPedometer (updates en vivo + query por rango), escritura HKWorkout y bridge ActivityKit; adapters JS MotionPort/HealthKitPort/LiveActivityPort hablan con el; el dominio no lo conoce. Previene: tres fronteras nativas divergentes, dependencias de terceros sin auditar, y serializar el estado de sesion por el bridge para la Live Activity. keep-awake y local-notifications quedan en plugins comunitarios verificados MIT
- (decision) DECISION 2 (Paul): doble canal — la PWA sigue viva en GitHub Pages como canal SECUNDARIO; la app Capacitor es el canal PRINCIPAL. El composition root detecta plataforma (Capacitor.isNativePlatform()) y registra adapters nativos cuando hay plugin, web si no; la PWA no recibe features que dependan de nativo y sigue degradando limpio como hoy. Previene: romper el despliegue vivo, perder la red de seguridad si la cuenta Apple caduca, y acoplar el codigo web a APIs de Capacitor en import time
- (decision) DECISION 3 (Paul): distribucion = builds locales de Xcode para el loop de desarrollo + TestFlight como canal estable de instalacion duradera (OTA, builds de 90 dias renovables). Cuenta Apple Developer de pago YA ACTIVA (confirma A-1 del spec). App Store queda fuera de scope. Previene: firma expirada sin Mac a mano; friccion de cable en cada actualizacion
- (version) Verificado npm 2026-07-28: @capacitor/core 8.4.2, @capacitor/cli 8.4.2 (requiere Node >=22), @capacitor/ios 8.4.2, @capacitor/local-notifications 8.2.1, @capacitor-community/keep-awake 8.0.1 — todos MIT. Capacitor 8 soporta iOS 15+ y requiere Xcode 26.0+ (capacitorjs.com/docs/ios)
- (decision) DECISION 4: iOS deployment target 16.1 — piso de ActivityKit (CAP-18); el dispositivo de Paul corre iOS 17+ (A-2 del spec). Previene: configurar un target que excluya la Live Activity o exija guards de disponibilidad en cada llamada
- (decision) DECISION 5: en nativo, los pasos de intervalos en background se reconstruyen por query CMPedometer (queryPedometerData por rango) como camino primario — el GapEstimator por cadencia queda SOLO como degradacion cuando la query no esta disponible. Previene: que un builder porte la estrategia PWA (estimar siempre) por inercia
- (decision) DECISION 6: la Live Activity se alimenta de eventos de pasos NATIVOS dentro del plugin (CMPedometer sigue entregando con WebView suspendido), NO por llamadas JS del WebView — en background el JS esta congelado. Previene: Live Activity congelada con el telefono bloqueado (el caso de uso central de CAP-18)
- (decision) DECISION 7: la app carga unicamente el bundle web local empaquetado en el build (sin server.url ni carga remota); las actualizaciones de la app llegan via nuevo build/TestFlight, no OTA. Previene: dependencia de red en runtime (romperia no-backend y offline) y deriva silenciosa entre lo probado y lo servido
- (decision) DECISION 8: HealthKit se escribe una sola vez al FINALIZAR la sesion (dato inmutable completo), con pantalla de pre-permiso antes del prompt del sistema; recordatorios de meta se reprograman de forma idempotente desde GoalEngine al finalizar sesion y al abrir la app. Previene: escrituras parciales/duplicadas en Salud y prompts de permiso en frio
- (decision) DECISION 9: el plugin custom vive en el repo como paquete local ./walktracker-kit (podspec propio, referencia file: en package.json, pnpm); ios/ se genera con cap add ios y se commitea. Previene: codigo nativo viviendo solo dentro de ios/App (regenerable) donde se pierde o diverge del control de versiones
- (event) Spine finalizado: lint limpio; ADs renumerados a AD-C1..C8 (evita colisión v3); correcciones de portación en AD-C1; MotionPort con distanceM; bloque UX en convenciones; canal web representado (StepDetector, DeviceMotionAdapter, WakeLockAdapter, MotionPort web); Inherited completa; AD-C5 gate fijado a Success signal; haptic deferred acotado; @capacitor/preferences en Stack; TestFlight versioning en convenciones; cláusula aspiracional AD-C3 movida a Deferred
