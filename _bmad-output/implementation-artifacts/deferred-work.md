# Deferred Work

- source_spec: `_bmad-output/implementation-artifacts/spec-8-6-extraccion-de-la-capa-nativa.md`
  summary: Borrar `WalkTracker/UI/Diagnostics/NativeLayerDiagnosticsView.swift` cuando exista la UI de sesión, y con ella deshacer su cableado — devolver `RootView` a no genérica (sin el parámetro `Diagnostics`, su `NavigationLink` ni la extensión `where Diagnostics == Never`) y quitar la rama `#if DEBUG` de `WalkTrackerApp`. Destino: un chore de limpieza propio, ya exigible: la UI de sesión existe desde la 1.4.
  evidence: La pantalla de diagnóstico es la superficie temporal de verificación en el iPhone de los cuatro adapters de la 8.6 (decisión de Paul, 2026-09-12); la UI de sesión la sustituye como consumidor real de los puertos.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-6-extraccion-de-la-capa-nativa.md`
  summary: Probar que `FeedbackAdapter.fire` solo reproduce sonido cuando `soundEnabled` es `true`, y con el `SystemSoundID` del evento. Destino: la historia 4.2, dueña de la preferencia de sonido.
  evidence: Hueco de verificación de la revisión de la 8.6 (V6): invertir la condición no lo detecta ningún test ni check manual. Falta un hook inyectable para `AudioServicesPlaySystemSound`; encaja con la historia 4.2, dueña de la preferencia de sonido.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-7-sustrato-de-verificacion-del-dominio.md`
  summary: **CERRADO el 2026-09-21 — su destino eran las historias 1.1–1.6 y las seis cerraron, cada una con su parte del reparto.** Estaba registrado así: Portar a mano a Swift Testing los escenarios de AD-6 —64 sitios de aserción / 163 ejecutadas, todos en `test/session-v3-tests.js` (secuencias de comandos sobre el agregado de sesión)—, cada uno en la historia del Epic 1 que porta la parte del agregado que ejercita. Reparto, según `WalkTrackerTests/Vectors/inventory.json`: 1.1: 14 · 1.2: 8 · 1.3: 11 · 1.4: 18 · 1.5: 3 · 1.6: 10.
  evidence: Separado de la 8.7 por decisión de Paul (2026-09-12): sin `Session` en Swift no hay runtime contra el que ejecutarlos, y portarlos en la 8.7 obligaría a escribir antes el agregado de las historias 1.1–1.4. La 8.7 los deja clasificados y citados, no portados.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-7-sustrato-de-verificacion-del-dominio.md`
  summary: Probar que el arranque termina (y nunca devuelve un catálogo parcial o vacío) cuando `achievements.json` no valida. Destino: la historia 3.2, la primera del Epic 3 que lee el catálogo de logros.
  evidence: Hueco de verificación de la revisión de la 8.7 (V3): sustituir el `fatalError` de `CompositionRoot.bundledAchievementCatalogOrTerminate()` por un catálogo vacío deja todos los tests en verde. Hace falta una costura (bundle y terminación inyectables); encaja cuando el Epic 3 lea el catálogo por primera vez.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-iniciar-sesion-cronometro-wall-clock.md`
  summary: Test automático de interfaz que compruebe que "Iniciar caminata" presenta la sesión a pantalla completa y que el tiempo en pantalla avanza cada segundo.
  evidence: Hueco de verificación de la revisión de la 1.1 (V2): sustituir el `TimelineView` por una lectura única congelaría el tiempo en 0:00 sin romper ningún test. **Reconciliado el 2026-09-20 (decisión D1):** esta entrada decía "el proyecto no tiene target de UI tests (XCUITest diferido en el spine)" y **XCUITest ya no está diferido: está descartado**, así que la salida que proponía no va a existir. La salida real es la de D1 — **extraer a un tipo probable sin SwiftUI** qué presenta la pantalla y con qué estado, y probarlo con la suite que ya existe: el destino de sesión de "Iniciar caminata" y la fuente del tiempo que pinta el cronómetro caben ahí. **Lo que no cabe** es que el tiempo *avance en pantalla* cada segundo: eso solo se ve renderizando, y pasa a **verificación manual en el iPhone 14**, no a esperar un arnés. Destino: la historia que vuelva a tocar Inicio o sesión, más el check manual.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-conteo-pasos-coprocesador.md`
  summary: Verificar que `HomeView` relee el permiso de Motion al volver a primer plano desde Ajustes y cierra la pantalla bloqueante a Inicio sin sesión.
  evidence: Hueco de verificación de la revisión de la 1.2 (VG, hallazgo 19): borrar el `.onChange(of: scenePhase)` o cambiar la fase deja todos los tests en verde, porque los tests llaman a `motionStatusMayHaveChanged()` directamente. **Reconciliado el 2026-09-20 (decisión D1):** esta entrada se apoyaba en "el proyecto no tiene target de UI tests", y ese target **no va a llegar** — XCUITest está descartado, no diferido. La salida de D1 sí sirve aquí, y además es barata: el **mapeo fase → intención** es lógica de presentación pura y sale de la vista a un tipo probable (el mismo arreglo que ya proponía el diferido de la 1.5), con lo que "cambiar la fase" deja de estar sin cubrir. Lo único que queda para **verificación manual** es el recorrido completo "denegar → Abrir Ajustes → conceder → volver", que atraviesa Ajustes del sistema y no lo cierra ningún arnés. Destino: el chore o la historia que extraiga la presentación de `HomeView`, más el check manual.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-metricas-en-vivo.md`
  summary: Comprobar si CoreMotion alterna muestras con y sin `distance` en una misma sesión; si lo hace, la distancia visible puede bajar (pasos → sistema menor) o congelarse (sistema → nil) y hay que reconciliar las dos fuentes. Destino: la caminata instrumentada de B-8, que es donde se registran las muestras; esto no lo zanja una historia, lo zanja una medición.
  evidence: Hallazgos 1, 2 y 26 de la revisión de la 1.3 (maybe-false, medium si es real). `Session.metrics(at:)` usa `systemDistanceM` en cuanto existe y los pasos solo si nunca llegó. Lo zanja registrar las muestras (`steps`, `distance`) de una caminata real en el iPhone 14.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-pausar-reanudar-finalizar.md`
  summary: **CERRADO el 2026-09-21 — ya tiene historia dueña: la Story 3.5 del Epic 3, creada por el correct course de ese día, que es exactamente lo que esta entrada pedía.** La causa raíz resultó ser que **no existe ningún FR para el resumen**: la UX lo pide en UX-DR4 y UX-DR5 y las historias se derivan de FRs, así que no había de dónde colgarlo. La 3.5 lo reclama citando la UX como origen, y es dueña de la composición y del contrato de filas, no de todo el contenido: la fila de Salud llega en la 6.1. Estaba registrado así: El resumen completo tras finalizar (Summary Screen de UX-DR4 con Salud, logros y celebración) no tiene historia dueña en `epics.md`; la 1.4 solo entrega un resumen mínimo (distancia, tiempo, pasos, ritmo, cadencia y "Volver a Inicio"). Hueco para un correct course que le asigne historia. Destino: el bmad-correct-course de 2026-09-21, que le asigna historia en epics.md.
  evidence: Decisión de Paul en la 1.4 (2026-09-13). `epics.md:96` lista "Summary Screen" en UX-DR4, pero ninguna historia la pide como entregable; las piezas que la llenarían viven repartidas en los epics 3 (logros, celebración), 5.1 (persistencia) y 6 (Salud).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-pausar-reanudar-finalizar.md`
  summary: Reconciliar la distancia entre tramos de pausa/reanudación cuando un tramo trae distancia del sistema y otro no: `distanceBaseM = systemDistanceM ?? 0` descarta la distancia derivada de pasos del tramo anterior. Destino: la caminata instrumentada de B-8, con la entrada de la 1.3: misma causa y mismas muestras.
  evidence: Hallazgos 1 y 5 de la revisión de la 1.4 (maybe-false, medium si es real). Misma causa que el diferido de la 1.3, pero cada reanudación abre un stream nuevo y lo hace alcanzable en cada pausa. Lo zanja registrar las muestras (`steps`, `distance`) de tramos reales en el iPhone 14.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-pausar-reanudar-finalizar.md`
  summary: Test automático de qué pantalla presenta `SessionView` según el estado (en curso, "En pausa" con Reanudar, resumen con métricas de `durationS`) y del respaldo `lastFinished` al cerrar el cover.
  evidence: Hallazgo 19 de la revisión de la 1.4 (VG): invertir el orden del `if`, quitar el respaldo o invertir `isPaused` en el título no rompe ningún test. **Reconciliado el 2026-09-20 (decisión D1):** decía "el proyecto no tiene arnés de tests de vista" dando por hecho que llegaría uno de XCUITest; **está descartado**. Esta entrada es, de las cinco del Epic 1, la que **mejor encaja en la salida de D1**: "qué pantalla presenta `SessionView` según el estado" es exactamente *qué pinta y con qué estado*, y sale entero a un tipo sin SwiftUI probado por la suite actual — el respaldo `lastFinished` incluido. No queda nada que dependa de renderizar. Destino: el chore o la historia que extraiga la presentación de `SessionView`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-reconstruccion-background.md`
  summary: Test automático del cableado de `scenePhase` en `RootView` (`.background` → `appDidEnterBackground()`, `.active` → `appDidBecomeActive()`, `.inactive` ignorado) y de lo que pinta la sesión al reconciliar (controles y "Descartar" deshabilitados, aviso tras 0,5 s, "~N" del resumen).
  evidence: Hallazgos de la revisión de la 1.5 (VG 1, BH): quitar el `.onChange`, intercambiar los casos o mapear `.inactive` a background deja todos los tests en verde, porque los tests del store llaman a las intenciones directamente. **Reconciliado el 2026-09-20 (decisión D1):** decía "el proyecto no tiene arnés de tests de vista" esperando XCUITest, que está **descartado**. El "arreglo barato" que esta misma entrada ya proponía —extraer el mapeo fase → intención a una función pura del store y probarla— **es** la salida de D1, y se adelantó a ella; lo mismo vale para lo que la sesión presenta al reconciliar (controles y "Descartar" deshabilitados, el "~N" del resumen), que es estado, no píxeles. **Queda para verificación manual** solo el aviso que aparece **tras 0,5 s**: es una temporización de presentación y se ve renderizando. Destino: el chore o la historia que extraiga la presentación de `RootView`/`SessionView`, más el check manual en el iPhone 14.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-reconstruccion-background.md`
  summary: Comprobar si, tras una consulta de reconciliación sin dato (nil o error, no timeout), el stream de CoreMotion entrega después el acumulado con los pasos del gap; si lo hace, esos pasos cuentan como medidos y como estimados (medium si es real, sin verificar). Destino: la caminata instrumentada de B-8, con las entradas de la 1.3 y la 1.4.
  evidence: Hallazgo BH 2 de la revisión de la 1.5 (maybe-false). El parche de la revisión cubre el caso en que el stream entrega DURANTE la reconciliación; no el de después. Lo zanja registrar en el iPhone 14 las muestras del stream tras una consulta nil (p. ej. forzando el camino sin dato) y ver si su acumulado incluye el gap.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-recuperacion-foreground.md`
  summary: Test automático de lo que pinta la recuperación: el `.task` de `WalkTrackerApp` que lanza `restoreOnLaunch()`, "Sesión recuperada" 3 s con su anuncio de VoiceOver, y el paso de `session.recovered` a `FinishedWalk` para el resumen "Caminata recuperada".
  evidence: Hallazgos de la revisión de la 1.6 (VG 3 y 4): quitar el `.task` o el `recovered: session.recovered` deja todos los tests en verde; `currentFinished` es código privado de vista. **Reconciliado el 2026-09-20 (decisión D1):** decía "no tiene arnés de tests de vista ni de arranque" contando con XCUITest, **descartado**. El "arreglo barato" que ya proponía —un `FinishedWalk(session:metrics:)` con test— **es** la salida de D1: el paso de `session.recovered` al resumen "Caminata recuperada" es lógica de presentación y sale a un tipo probable. **No cubre** el `.task` de arranque de `WalkTrackerApp` —es cableado del ciclo de vida de la app, no presentación—, ni que "Sesión recuperada" **dure 3 s** con su anuncio de VoiceOver: las dos pasan a **verificación manual en el iPhone 14**. Destino: el chore o la historia que extraiga la presentación del resumen, más los checks manuales.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-gate-success-signal.md`
  summary: Retirar o condicionar el registro de medición `WTM1` (`MeasurementLog`, categoría `Medicion`, nivel `notice`, valores `public`) cuando el gate 8.4 haya cerrado, antes de cualquier build que no sea de medición. Destino: el chore A-2.
  evidence: Hallazgo de la revisión de la 8.4 (BH): cada muestra, consulta y transición queda persistida en el log del sistema, visible en sysdiagnoses, en todo build Release (TestFlight y un eventual App Store). La decisión de Paul fijó `OSLog` público para extraer la caminata, pero no dijo qué pasa después; ninguna tarea lo retira. Cabe retirarlo antes si la 8.4 cierra primero, pero el dueño es el chore A-2.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-gate-success-signal.md`
  summary: Tests de lo que el informe no distingue hoy: una línea `estimate` con `steps=0` escrita por el store (el `GapEstimator` corrió y no sumó) y un p95 de duración de consulta distinto del máximo (fixture con ≥ 20 consultas). Destino: el chore A-2, junto a la entrada de `WTM1`.
  evidence: Hallazgos de la revisión de la 8.4 (VG 3 y 4): mover el `measure` bajo `guard estimated > 0` o sustituir `percentile(ms, 95)` por el máximo deja todo en verde. Informativo en el gate: una caminata sin tocar la pantalla da dos consultas (p95 = máx.) y se espera sin estimaciones; la regla del timeout usa el máximo.
- source_spec: `_bmad-output/implementation-artifacts/spec-retro-e1-a6-gates-arquitectura.md`
  summary: La sección 4 de `check-project-shape.sh` (frameworks prohibidos en `Domain/`) no reconoce imports con atributo o nivel de acceso (`@preconcurrency import CoreMotion`, `internal import SwiftUI`); reutilizar ahí el `import_re` de las secciones 7 y 10. Y que el texto de la sección 10 nombre el módulo real esté cubierto por un caso rojo. Destino: el siguiente chore de gates de la familia A-6 / B-5 / B-6.
  evidence: Revisión del A-6 (EC, verificado: `@preconcurrency import CoreMotion` en `Domain/` sale en verde; VG: sustituir el módulo del mensaje por `x` deja los 36 casos verdes). La spec del A-6 prohibía tocar las secciones 1–6, así que queda fuera de ese chore. **Re-confirmado el 2026-09-18** por el chore de tokens al añadir la sección 12: `@preconcurrency import CoreMotion` en `Domain/` sigue saliendo en verde. La sección 12 no lo necesita —no mira imports— y no tocó la 4.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-clima-open-meteo.md`
  summary: Recordar entre lanzamientos que Paul eligió "Ahora no" en la pre-pantalla de ubicación de la 2.1 (hoy solo dura la ejecución, en `SessionStore.declinedLocationPromptThisLaunch`) y ofrecer el permiso de clima desde Ajustes. **Re-apuntado por segunda vez, ahora a la 2.1bis / la historia que vuelva a tocar el flujo del clima** por la 2.3 (2026-09-19). Sigue siendo un campo más en `AppSettings` y una intención más en `SettingsStore`, y ahora además hay pantalla donde ofrecerlo.
  evidence: **Las dos piezas que faltaban ya existen, y aun así queda fuera de alcance.** La 2.2 dejó `settings.json`, `AppSettings`, `SettingsFileAdapter` y `SettingsStore`; la 2.3 añade la pestaña Ajustes (`WalkTracker/UI/Settings/SettingsView.swift`) y el patrón de "un campo de `AppSettings` con su intención en `SettingsStore+Stride.swift`", que es exactamente el molde que este diferido necesita. Lo que lo bloquea ahora es **solo** la mitad que toca el flujo del clima: la Intent congelada de la 2.3 lo prohíbe explícitamente ("Never: … tocar el flujo de clima de la 2.1, ni cerrar aquí el diferido del 'Ahora no' de la ubicación: su mitad de persistencia ya no está bloqueada, pero la otra mitad toca la 2.1. Se re-apunta con razón"). Y la 2.3 decidió además que **Ajustes nace con un único campo**, así que ofrecer el permiso desde aquí tampoco entraba. Destino: la historia que vuelva a tocar la captura de clima, o la retro del Epic 2.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-clima-open-meteo.md`
  summary: Probar el camino real de `LocationAdapter.readLocation` y `requestPermission` (ubicación cacheada fresca o caducada, timeout, lectura sustituida, respuesta tras el timeout, peticiones de permiso duplicadas) con una costura sobre `CLLocationManager`. Destino: el chore que introduzca la costura sobre `CLLocationManager`.
  evidence: Revisión de la 2.1 (VG 3, BH): quitar o invertir `isFresh` en `readLocation` deja todo en verde; los tests solo cubren los helpers estáticos y el store usa `LocationStub`. Hace falta una fuente de ubicación inyectable; hoy lo cubren los checks manuales en el iPhone 14.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-clima-open-meteo.md`
  summary: Unificar `WeatherCondition.rain` (2.1, `Domain/Weather`) con `WeatherCategory.rain` (catálogo de logros, `Domain/Achievements`) cuando el Epic 3 evalúe `rain_walker`, y leer los códigos WMO de lluvia de `evaluateAchievements.json` en el test en vez de una lista escrita a mano. Destino: 3.2.
  evidence: Revisión de la 2.1 (BH): dos enums de lluvia sin nada que los sincronice; el test "casos de lluvia de evaluateAchievements.json" usa una lista propia con códigos que el fichero no tiene. Lección L2 de la retro del Epic 1.
- source_spec: `_bmad-output/implementation-artifacts/spec-r1-reconstruccion-background.md`
  summary: Sustituir la pareja de campos `backgroundedAt` y `stepsMeasuredAtGapStart` del `SessionStore` por un único opcional `struct PendingGap { let start: Date; let stepsMeasured: Int }`, para que el invariante "o están los dos o no está ninguno" sea irrepresentable roto en lugar de sostenerse a mano. Destino: el chore A-1.
  evidence: Hallazgo de la revisión del arreglo R1 (estructura): hoy los dos campos se fijan y se limpian juntos en seis sitios (`appDidEnterBackground()`, `appDidBecomeActive()`, `confirmFinish()`, `resetSessionState()` y dos veces en `restoreOnLaunch()`), y lo único que empareja es un comentario en cada uno; olvidar uno deja el gap medio abierto y `reconcile(until:)` sale por el `guard` sin estimar y sin decir por qué. Enlaza con la lección L3 de la retro del Epic 1 (campos compartidos del store) y con el action item A-1.
- source_spec: `_bmad-output/implementation-artifacts/spec-ux-tokens-nativos.md`
  summary: Unificar los tres mecanismos con que la UI pinta una superficie propia — `.glassEffect` (`LocationPermissionView`), `.background(.fill.quaternary, in:)` (`WeatherCard`) y `.background(Colors.estimated.opacity(0.12), in:)` (el aviso de estimados de `SessionView`) — y adoptar `ConcentricRectangle`, que AD-13 nombra explícitamente y que hoy no usa ninguna: las tres pasan una `.rect(cornerRadius:)`, que no concuerda con la curvatura del contenedor.
  evidence: Hallazgo del chore de tokens (2026-09-18). El chore unificó el radio (20) y el relleno (16/12), que es lo que se podía mover sin cambiar el aspecto; elegir un solo mecanismo cambia el material de dos de las tres superficies y es un cambio visual real, fuera de su alcance ("Never: rediseñar pantallas"). Destino: la historia que estrene una superficie propia nueva — el anillo de meta (3.1) o la tarjeta de logro (3.3).
- source_spec: `_bmad-output/implementation-artifacts/spec-ux-tokens-nativos.md`
  summary: Respetar Reduce Motion en las vistas que hoy no lo consultan: `HomeView`, `SessionSummaryView`, `PermissionScreen` y `WeatherCard` no leen `accessibilityReduceMotion`; solo `SessionView` lo hace. Hoy ninguna anima, así que no hay defecto visible, pero nada garantiza que la primera animación que entre lo respete. Destino: la historia 3.3, la primera que anima —y la 3.1 si el anillo de meta llega antes.
  evidence: Hallazgo del chore de tokens (2026-09-18), contra la convención de accesibilidad del spine ("Reduce Motion respetado") y UX-DR6. Arreglarlo hoy sería añadir código sin conducta que lo ejercite; el sitio natural es la historia que introduzca la primera animación en esas pantallas — la celebración de logro (3.3) o el anillo de meta (3.1).
- source_spec: `_bmad-output/implementation-artifacts/spec-ux-tokens-nativos.md`
  summary: Llevar el vocabulario visual a `WalkTrackerActivity/` (la extensión de Live Activity), que se queda fuera de este chore entero: cablea `spacing: 16`, `spacing: 2` y `.padding()`, la sección 12 del gate **no la escanea** (solo mira `WalkTracker/UI/`), y no puede importar los tokens porque solo depende de `Shared` (AD-15) — el fichero vive en el target de la app. Tampoco tiene la key del acento, así que la app sale verde lima y su Live Activity se queda con el azul del sistema. Salidas posibles: mover los tokens a `Shared/` (los usarían los dos targets) o duplicarlos con un test que los ate. Destino: la historia que vuelva a tocar la Live Activity. **Ampliado el 2026-09-20 por B-2:** la sección 12 ya no veta un nombre sino los doce colores cromáticos del sistema, y sigue sin escanear `WalkTrackerActivity/`, así que la extensión puede escribir `.red` o `.mint` sin que falle nada. Hoy no nombra ninguno (verificado con la misma regex del gate sobre `WalkTrackerActivity/`, `Shared/`, `Application/`, `Adapters/` y `App/`: cero coincidencias), así que es una puerta abierta, no un defecto vivo.
  evidence: Hallazgo del chore de tokens (2026-09-18). La Intent lo pone fuera de alcance explícitamente ("Never: tocar … la extensión de Live Activity"), y el `sources:` de `WalkTrackerActivity` no alcanza `WalkTracker/UI/`, así que ni el gate ni el compilador ven el problema: es una divergencia visual que no falla nada.
- source_spec: `_bmad-output/implementation-artifacts/spec-ux-tokens-nativos.md`
  summary: Verificar que `minimumScaleFactor(0.5)` con `lineLimit(1)` no trunca la cifra del héroe ni la de una celda de la rejilla a tamaño de accesibilidad máximo. **Re-apuntado el 2026-09-20 (decisión D1):** decía "exige renderizar una vista y medir, y no hay target de UI tests (A-4 abierto)"; **A-4 está cerrado y XCUITest descartado**, y medir píxeles en pantalla es justo lo que el arnés de tipos probables no hace. Destino: **verificación manual en el iPhone 14** a tamaño de accesibilidad máximo, no un arnés.
  evidence: Hallazgo del chore de tokens (2026-09-18). El chore reconcilió los tres `minimumScaleFactor` de `SessionView` (0,3 / 0,4 / 0,5) en 0,5 con un criterio escrito: un texto se encoge hasta la mitad, nunca más. Es un **quinto cambio visible** —además de los cuatro decididos—, solo a tamaños grandes, y **no lo cubre ningún test**: el peor caso real se estimó en ~0,85 sobre el papel, no medido en pantalla.
- source_spec: `_bmad-output/implementation-artifacts/spec-ux-tokens-nativos.md`
  summary: Límite conocido de la sección 12 del gate: un color partido en varias líneas la esquiva (`Color(` en una línea y `red: 0.8,` en la siguiente). Es inherente a un gate que trabaja línea a línea; cerrarlo pide unir líneas lógicas antes de buscar, o un parser. Destino: el chore que reescriba la sección 12 sobre líneas lógicas; hoy sin dueño.
  evidence: Hallazgo del chore de tokens (2026-09-18). `code_lines` emite `fichero:línea:código` y todas las reglas buscan dentro de una línea, así que la evasión no es un descuido de una regex concreta: la comparten las siete reglas. Se registra para que no se confunda "el gate pasa" con "no hay colores cableados".
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-frase-motivacional.md`
  summary: Verificar en un test lo que la frase motivacional **presenta**: que `QuoteOverlay` se presenta sobre la sesión en curso sin tapar su funcionamiento, que **se auto-descarta a los 3 s** y que **un tap en cualquier punto lo adelanta** (incluido el tap sobre el propio texto, que el `ScrollView` del centro se comía hasta el arreglo de la revisión), que VoiceOver lo toma como contenido **modal** (`.isModal`) y lee la frase **una sola vez**, que con **Reduce Motion** aparece sin fundido, y que con la **frase más larga** del banco a tamaño de accesibilidad máximo no se recorta (se desplaza).
  evidence: Hueco de verificación declarado en la propia spec de la 2.2 (Design Notes, "Lo que no se puede verificar hoy") y reconfirmado en su revisión. **Reconciliado el 2026-09-20 (decisión D1):** decía "el proyecto sigue sin target de UI tests (A-4, abierto)", y **A-4 está cerrado**: la decisión fue **descartar XCUITest** y extraer la lógica de presentación a tipos probables. **Esta entrada es justamente el caso que D1 deja escrito que NO cierra**: que el overlay se presente sobre la sesión *sin tapar su funcionamiento* es un **solape entre capas** —el mismo problema que B-4, la frase tapando la pre-pantalla de ubicación— y solo se ve renderizando. Por eso pasa, entera, a **verificación manual en el iPhone 14**: el auto-descarte a los 3 s, el tap en cualquier punto, la modalidad para VoiceOver, Reduce Motion y la frase más larga a tamaño de accesibilidad máximo se comprueban mirando la pantalla, no esperando un arnés que no va a llegar. Destino: los checks manuales de la próxima caminata (los mismos de D2/`B-8`). **Mitigación con precedente:** la decisión vive en el store —`store.quote`, `dismissQuote()`, que `confirmFinish()` lo apaga, y el invariante de que una sesión restaurada no lo enciende— y eso sí lo cubre `SessionStoreMotivationTests`; borrar el `.task(id:)` del overlay, quitar uno de los dos `onTapGesture` o invertir `reduceMotion` en `SessionView` no rompe ningún test. Hoy lo cubren los checks manuales en el iPhone.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-frase-motivacional.md`
  summary: Reclasificar los tres sitios `math-random` de `WalkTrackerTests/Vectors/inventory.json` (`test/motivation-tests.js:20, :27, :32`), hoy excluidos por depender de `Math.random`, ahora que `MotivationEngine` recibe el azar por `RandomPort` y esas conductas SÍ son deterministas en Swift. Tocar `baseline` y `totals` del inventario es trabajo aparte.
  evidence: La Intent congelada de la 2.2 lo prohíbe explícitamente ("Never: recategorizar los sitios `math-random` del inventario de vectores: toca `baseline` y `totals`, y es trabajo aparte"). Las tres conductas están cubiertas en Swift por `MotivationEngineTests` (caso normal, ventana vacía, todas excluidas), así que no hay hueco de cobertura real: lo que queda es que el inventario de AD-6 diga la verdad sobre por qué están fuera. Destino: un chore del inventario, o la retro del Epic 2.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-frase-motivacional.md`
  summary: Desacoplar la referencia v3 archivada (`index.html`, `sw.js`, `test/motivation-tests.js`) del árbol de la app iOS: las tres cargan hoy `./WalkTracker/Resources/quotes.json`, una ruta **dentro** del target de la app. Salidas posibles: una copia propia del banco para la referencia, un enlace, o declarar la referencia congelada y sacarla de la raíz.
  evidence: La 2.2 movió `quotes.json` de la raíz a `WalkTracker/Resources/` para que entrara en el bundle por `project.yml`, y actualizó las tres rutas que lo cargaban: sin eso la suite legacy y la PWA se habrían quedado con un 404 que ningún gate mira. Funciona, y `test/motivation-tests.js` sí está cubierto por `check-inventory`, pero **acopla dos cosas que deberían poder moverse por separado**: reorganizar `WalkTracker/Resources/` rompería la referencia v3, y nadie lo esperaría. Hallazgo de la revisión de la 2.2. Destino: el chore que archive o retire la referencia v3, o la retro del Epic 2.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-frase-motivacional.md`
  summary: **CERRADO el 2026-09-20 por B-6.** Riesgo conocido del gate del dominio: la lista de `-only-testing:` de `Scripts/verify-domain.sh` (15 suites entonces) era **explícita y se mantenía a mano**. Un suite nuevo que nadie añadiera no se ejecutaba en el gate y **nadie se enteraba**: el gate salía verde igual. Se cerró por la segunda de las dos salidas que esta entrada proponía: `verify-domain.sh` deriva los `@Suite` de `WalkTrackerTests/{Domain,Vectors,Scenarios}` recorriendo el árbol y **falla nombrando** cualquiera que no esté en `DOMAIN_SUITES` —y cualquiera de `DOMAIN_SUITES` que ya no esté en el árbol—, antes de arrancar el simulador. Su camino rojo vive en `Scripts/verify-domain-tests.sh` y lo ejecuta el propio gate. **Al derivarla salió el hallazgo que esta entrada predecía:** `WorkoutRecordTests` (`WalkTrackerTests/Domain/`) llevaba sin ejecutarse en el gate desde que existe; la lista pasa de 15 a 16.
  evidence: El propio script lo advierte en un comentario desde la 8.7 ("el proyecto no se versiona y XcodeGen fija la lista de ficheros: un vector nuevo sin regenerar no entraría en el bundle de tests y no se ejecutaría EN SILENCIO"), y la 2.2 lo vivió tres veces: `MotivationEngineTests`, `QuoteBankTests` y `AppSettingsTests` hubo que añadirlos a mano. La convención del repo es que un riesgo conocido va aquí con destino, no que se quede como comentario en el script (lección L2). Destino: la retro del Epic 2, o el chore que toque los gates.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-recalibracion-zancada.md`
  summary: Verificar en un test el **contrato vista↔store** de Ajustes y lo que la pantalla presenta: que Guardar llama a la intención, que editar apaga el mensaje, que salir de la pantalla lo apaga, que cada resultado pinta **su** texto, que el botón "Usar el valor por defecto" llama a `clearStride()`, que el campo se ve y se puede escribir, que el teclado decimal sale (con la coma del teclado en español) y se cierra con "Listo" o al desplazar, que el objetivo táctil del campo mide 44 pt de verdad, que VoiceOver lee etiqueta, valor y error **y oye el anuncio del resultado**, y que con Dynamic Type al máximo no se recorta nada. Destino: el chore que extraiga la presentación a tipos probables, con los diferidos de la 1.1 y la 1.2.
  evidence: **Reconciliado el 2026-09-20 (decisión D1):** decía "exige renderizar una vista y el proyecto sigue sin target de UI tests (A-4, abierto)", y **A-4 está cerrado**, con XCUITest **descartado**. La entrada se parte en dos: el **contrato vista↔store** —que Guardar llama a la intención, que editar y salir apagan el mensaje, que cada resultado pinta *su* texto, que "Usar el valor por defecto" llama a `clearStride()`— es lógica de presentación y sale a un tipo probable con la suite actual, que es la salida de D1. Lo **físico** —que el campo se vea y se pueda escribir, el teclado decimal con la coma del español y su "Listo", que el objetivo táctil mida 44 pt **de verdad**, que VoiceOver lea etiqueta, valor y error y **oiga** el anuncio, y que Dynamic Type al máximo no recorte— solo se ve renderizando y pasa a **verificación manual en el iPhone 14**. **Mitigación con precedente:** toda la decisión vive en el store —`saveStride(fromText:)`, `clearStride()`, `strideEditingDidChange()`, `strideScreenDidDisappear()` y `strideOutcome`— y eso lo cubre `SettingsStoreStrideTests`; el ida y vuelta del campo y que cada resultado tenga un texto distinto los cubre `SettingsViewTests` sin renderizar. Lo que **no** cubre ningún test sigue siendo el cableado de la vista: borrar `settingsStore.saveStride(fromText: text)` del botón Guardar, quitar el `.onDisappear`, dejar de llamar a `strideEditingDidChange()` desde el binding del campo, quitarle al `TextField` el `.frame(minHeight: LayoutMetrics.touchTargetMin)` o intercambiar dos ramas del `switch` de mensajes deja toda la suite verde. Revisado el 2026-09-19: esta entrada citaba un `.onChange(of: text)` y un marco en la fila que la revisión de la 2.3 movió de sitio. Hoy lo cubren los checks manuales en el iPhone.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-recalibracion-zancada.md`
  summary: Probar que recalibrar la zancada **no cambia el historial** de caminatas ya cerradas. La 5.1, que crea `sessions.json`, **ya está cerrada y mergeada** (eb124a4), así que `SessionRecord` y el historial persistido existen y el test que faltaba —cerrar una sesión con 0,655, recalibrar a 0,670 y comprobar que la fila guardada sigue en 0,655— se puede escribir hoy. Destino: un chore de test inmediato.
  evidence: **No es demostrable en la 2.3**, y la propia spec lo dice (Design Notes): no existe registro de sesiones cerradas hasta la 5.1, así que hoy un test solo podría comprobar que `Session.strideM` es un `let`, cosa que ya garantiza el compilador. Lo que sí queda probado ahora es la mitad alcanzable —que la sesión **viva** conserva su zancada congelada y que su snapshot no se reescribe al recalibrar (`SessionStoreTests.liveSessionKeepsItsFrozenStride`)—. Cuando la 5.1 escriba el historial, el test que falta es: cerrar una sesión con 0,655, recalibrar a 0,670 y comprobar que la fila guardada sigue en 0,655.
- source_spec: `_bmad-output/implementation-artifacts/spec-b1-ajustes-no-se-pisan.md`
  summary: Contarle a Paul que los ajustes no se pudieron leer. Con la lectura fallida, Ajustes muestra los valores por omisión —el campo de zancada sale vacío aunque en disco haya un 0,72— y solo se entera si pulsa Guardar, que responde "no se pudo guardar". Falta el aviso en la pantalla ("no se pudieron leer los ajustes; no se tocará el fichero") y, con él, la decisión de si ofrecer un reintento explícito o el camino de "empezar de cero". Destino: la historia que vuelva a tocar Ajustes (Epic 3 trae la meta semanal, la 4.2 el sonido).
  evidence: B-1 cierra la pérdida de datos —el fichero ya no se sobrescribe— y reutiliza el resultado `notPersisted` que la 2.3 ya pintaba, que es lo que la Intent congelada pedía ("el resultado observable que ya existe se reutiliza"). Lo que no cubre es el estado **previo** a pulsar nada: `SettingsStore.readOutcome` ya distingue los tres casos y nadie lo pinta, así que la pantalla no distingue "nunca recalibraste" de "no pude leer tu recalibración". Exige texto nuevo en el String Catalog y una fila más en la pantalla, y la Intent de B-1 acota el alcance al store ("Never: bloquear, retrasar o condicionar el arranque de una caminata"; el chore es de persistencia). El reintento automático de `save(applying:)` cubre el caso común sin UI.
- source_spec: `_bmad-output/implementation-artifacts/spec-b1-ajustes-no-se-pisan.md`
  summary: Los dos suites que cubren el ciclo de vida de `settings.json` —`SettingsStoreTests` (dentro de `WalkTrackerTests/Adapters/SettingsFileAdapterTests.swift`) y el nuevo `SettingsStorePersistenceTests` (`WalkTrackerTests/Application/`)— **no están en la lista de `-only-testing:` de `Scripts/verify-domain.sh`**, y es deliberado: el gate es de `Domain/` y `Vectors/` desde la 1.1 (hallazgo B3, rechazado) y la 2.3 lo volvió a aplicar. Los ejecuta la suite completa. Si alguna vez se quiere un gate rápido del seam adapter↔store, hace falta decidir dónde vive. Destino: el chore que decida si existe un gate rápido del seam adapter↔store y dónde vive.
  evidence: Hallazgo D10 de la retro del Epic 2, recordado por la Code Map de B-1 ("comprobar si deben entrar en el gate y, si entran, añadirlos a mano"). Verificado allí contando los casos del `.xcresult`: 622 en 50 suites, con los 10 casos del suite nuevo y los 9 de `SettingsStoreTests` ejecutados por nombre. **Actualizado el 2026-09-20 por B-6:** lo de fondo —que la lista se mantenía a mano y añadir un suite a un fichero ya listado no lo metía en ningún gate— **está cerrado**: el gate deriva los suites del árbol y falla nombrando el que falte. Lo que sigue abierto es solo la decisión de ALCANCE: los dos suites de `settings.json` viven en `Adapters/` y `Application/`, fuera de `WalkTrackerTests/{Domain,Vectors,Scenarios}`, así que el inventario derivado no los mira y los ejecuta la suite completa. Si alguna vez se quiere un gate rápido del seam adapter↔store, sigue haciendo falta decidir dónde vive.
- source_spec: `_bmad-output/implementation-artifacts/spec-b3-zancada-representable.md`
  summary: Nadie **registra ni pinta** `SessionMetrics.degraded`. El `catch` de `Session.metrics(at:)` ya no mata el proceso: degrada —distancia 0, sin ritmo, la cadencia que se pueda— y lo marca en el valor, que es lo único que el dominio puede hacer sin un logger. Falta decidir quién lo escribe en `OSLog` (`SessionStore` es el único que llama a `metrics(at:)`, 13 veces, y tiene `Logger`) y si la pantalla debe decir algo. **Re-apuntado el 2026-09-21 (B-9), y partido en dos porque son dos decisiones distintas:** (a) **el registro → `B-10`**, el action item que unifica "un nivel de log por clase de suceso" en `SessionStore` (D7 de la retro del Epic 2); es exactamente su trabajo y su fichero, y hacerlo fuera de él volvería a inventar un nivel de log por historia, que es el defecto que B-10 cierra. (b) **si la pantalla lo dice → decisión de producto, owner Paul**, a responder cuando B-10 aterrice o, como muy tarde, en la retro del Epic 3; es AD-22 ("celebrar, nunca culpar") aplicado a un caso que hoy nadie ha visto ocurrir, y ninguna historia pendiente lo necesita para avanzar. Destino: el chore B-10 para el registro, y la decisión de producto de Paul para si la pantalla lo dice.
  evidence: La Intent congelada de B-3 pide que el `catch` "degrade y lo registre". La degradación está cerrada y probada (`MetricsScenarios`, "metrics(at:) ante un cálculo imposible DEGRADA"); el registro se quedó en el valor porque `Domain/` solo importa `Foundation` (AD-3) y meter `OSLog` en el dominio sería un precedente nuevo —el proyecto hace entrar las capacidades por un puerto (`ClockPort`, `RandomPort`)— y una renegociación de arquitectura dentro de un chore, que la Intent acota a tres arreglos. La lectura tolerante sí quedó registrada, porque esa vive en un adapter que ya tiene `Logger` (`SettingsFileAdapter.decode`). **Por qué B-9 no lo cerró, dicho en vez de callado:** su destino anterior era "**B-9**, o la historia que vuelva a tocar las métricas en vivo", y B-9 acabó siendo un chore de **documentos y atribución** cuyo bloque congelado prohíbe expresamente tocar la lógica de la sesión ("Never: tocar la lógica del clima, el campo de zancada ni el flujo de permisos"; el alcance de código es una sección de Ajustes). La reconciliación de S6 que B-9 sí hizo —enmendar AD-11 y no escribir `DegradationPolicy`— **cambia además el argumento**: al no existir un sitio único donde vivan las degradaciones, registrar ésta pasa a ser un caso más del vocabulario transversal, que es literalmente el encargo de B-10. Esta entrada queda **re-apuntada por segunda vez y con dueño nombrado en las dos mitades**: es la lección L2, y dejarla otra vez en "la historia que vuelva a tocar X" sería huérfana por tercera vez.
- source_spec: `_bmad-output/implementation-artifacts/spec-b3-zancada-representable.md`
  summary: `MetricsCalculator.maxRepresentableStrideM` se deriva del tope de pasos del **agregado de sesión** (`maxSteps` = 2⁶⁴, los dos `Int` en su máximo). Si alguna vez la distancia se calcula sobre otra cosa —el historial acumulado de la 5.1, una suma de varias sesiones—, el tope de pasos deja de ser el factor correcto y el límite hay que rederivarlo. Está escrito en su doc, pero **ningún gate lo comprueba**: nada impide que aparezca un segundo llamante de `distanceM` con más pasos. Destino: la historia 3.1, la primera que suma distancia sobre el historial.
  evidence: El límite es deliberadamente **exacto**, sin holgura: con `maxRepresentableStrideM` la fórmula da un número y con su `.nextUp` no, y eso lo fija un test (`MetricsScenarios`, "el límite de la zancada es el que impone la aritmética"). Esa exactitud es lo que hace honesto el motivo "no cabe" que ve Paul, y también lo que lo deja sin margen si el número de pasos de la fórmula crece. Hoy `distanceM` tiene dos llamantes, los dos en `Session.metrics(at:)`, y el propio calculador valida su resultado, así que el fallo sería un rechazo visible y no un `inf`: por eso se registra en vez de inventar un factor de seguridad.
- source_spec: `_bmad-output/implementation-artifacts/spec-b2-color-de-error.md`
  summary: Nada ata el token `Colors.error` a la **rama** que debe pintarlo. `SettingsView.style(for:)` reparte cuatro resultados entre `.secondary`, `.primary` y `Colors.error`, y **intercambiar dos ramas deja toda la suite y el gate en verde**: pintar de rojo el aviso de rango humano —que no es un error, y que la 2.3 decidió distinguir por jerarquía— no lo detecta nadie. El test mide el **color**; el gate prohíbe **nombrar** un cromático del sistema; el **reparto** no lo cubre ninguno de los dos. Cerrarlo pide o renderizar la vista o cambiar la forma de `style(for:)` para que devuelva algo comparable, y lo segundo es un refactor de la pantalla que la Intent congelada de B-2 acota fuera ("Never: cambiar los textos del mensaje ni la estructura de la pantalla"). **Reconciliado el 2026-09-20 (decisión D1):** el destino decía "A-4, o la historia que vuelva a tocar Ajustes", y **A-4 está cerrado** — la decisión fue descartar XCUITest y extraer la presentación a tipos probables. Eso **sí** cierra esta entrada, y por la segunda vía, no por la primera: hacer que `style(for:)` viva en el tipo probable de `SettingsView` y devuelva algo comparable es exactamente lo que D1 prescribe, y entonces intercambiar dos ramas rompe un test. Destino: la historia o el chore que extraiga la presentación de Ajustes.
  evidence: B-2 cierra los dos mecanismos que la retro señaló (D2, V3): el rol "color de un error" es token medido en `DesignTokensTests` —cuatro fondos reales, control negativo con `Color.red` del sistema, separación de tono frente al acento y al estimado— y la sección 12 del gate pasó de vetar `.orange` por su nombre a vetar los doce cromáticos del sistema. Lo que sigue sin cobertura es el mismo hueco de siempre: el cableado de la vista, ya registrado para el resto de Ajustes en el diferido de la 2.3 ("borrar `settingsStore.saveStride(fromText: text)` del botón Guardar… o intercambiar dos ramas del `switch` de mensajes deja toda la suite verde"). Esta entrada lo nombra para el color, que es el criterio de aceptación "el aviso de rango no usa el color de error": hoy lo sostiene una lectura del código, no un test.
- source_spec: `_bmad-output/implementation-artifacts/spec-a5-reconciliar-planificacion.md`
  summary: `WakeLockPort` **no tiene historia dueña**. El conjunto cerrado de 11 puertos de AD-10 tiene 9 escritos: falta `NotificationPort`, que sí tiene dueño (historia 6.2), y falta `WakeLockPort`, que no lo tiene. Lo que falta por decidir es la **pregunta abierta Q-3** de la retrospectiva del Epic 1 — *¿sigue siendo necesario, ahora que el conteo funciona con la pantalla bloqueada? Si lo es, ¿en qué epic?*—, y el owner de la respuesta es **Paul**: hasta que la responda, ninguna historia lo asume y AD-10 queda con dos miembros sin implementar. Destino: la pregunta abierta Q-3 de la retro del Epic 1, owner Paul.
  evidence: Este chore corrigió la afirmación falsa de `epics.md` (fila de la 8.2 anulada), que decía que *"el wake lock sobrevive como `WakeLockPort` (AD-10) en Epic 1"*: el Epic 1 cerró con sus seis historias en `done` y sin él, y la 1.6 lo excluye en sus Boundaries. La Intent congelada de este chore prohíbe decidirlo aquí (*"Never: decidir el `WakeLockPort` … qué debe pasar con él es la pregunta abierta Q-3"*), así que lo único que se hizo fue dejar escrito que está abierto, en `epics.md` y en AD-10. **Esta entrada existe porque es exactamente el caso de la lección L2**: `WakeLockPort` "en Epic 1" se perdió la primera vez por vivir solo en un documento de planificación y no en `deferred-work.md`, que es lo único que la historia siguiente lee.
- source_spec: `_bmad-output/implementation-artifacts/spec-a5-reconciliar-planificacion.md`
  summary: **CERRADO el 2026-09-20 — Paul amplió el chore al paquete SPEC y las líneas ya están enmendadas.** Quedaba que `capabilities.md` y `domain-model.md §5` dieran la semana ISO en UTC y que `achievements.md` recortara las franjas de los logros. Se corrigieron con su fuente, junto con otras seis afirmaciones desfasadas del mismo tipo que salieron al revisar el paquete entero (autosave del snapshot, entrada y defensas del `GapEstimator` tras R1, descarte global de estimados, reglas de la zancada tras B-3, toolchain de OQ-5 y la atribución de Open-Meteo, esta última marcada **abierta con dueño B-9**, no resuelta). No queda trabajo: se deja la entrada como rastro de que el desfase del paquete SPEC se cerró, y de por dónde entró.
  evidence: Descubierto al verificar el criterio de cierre de A-5 (*"el contexto del Epic 3 compilado después de este chore dice hora local y 05:00–07:59"*), cuando la Intent congelada todavía acotaba el chore a tres documentos de planificación. **El paquete SPEC nunca se había revisado contra lo construido** y arrastraba la misma obsolescencia un nivel más arriba, con el agravante de que el compilador de contexto de épica también lo lee. `AD-19` sigue citando `domain-model.md §5` como el sitio de la contradicción UTC/local: §5 conserva ahora una nota fechada que dice que se resolvió a favor de §9, para que esa cita siga aterrizando en algo.
- source_spec: `_bmad-output/implementation-artifacts/spec-a5-reconciliar-planificacion.md`
  summary: La regla de `speed_walker` **no cabe entera en el catálogo de datos**. `achievements.md` la especifica como *"`session.paceSecPerKm` > 0 y < 480"*, pero `achievements.json` solo puede expresar la mitad: `{ "metric": "paceSecPerKm", "threshold": 480, "comparison": "lt" }`. El esquema de AD-5 no tiene forma de escribir la guarda inferior, así que **alguien tiene que decidir dónde vive**: si el evaluador trata el ritmo ausente o ≤ 0 como "no cumple" por ser `nil` (el dominio da `paceSecPerKm = nil` con menos de 100 m, no 0), si `between` cubre el caso con `[1, 479]`, o si la guarda es una rama del `switch` del evaluador. Es una **decisión pendiente, no un arreglo**: aquí no se toca ninguno de los dos ficheros. Destino: la historia del Epic 3 que escriba el `AchievementEngine` (**3.2**), que es la primera que lo va a necesitar.
  evidence: Encontrado al contrastar las 14 filas de `achievements.md` contra `WalkTracker/Resources/achievements.json` en el chore A-5 ampliado al paquete SPEC. Las otras trece filas cuadran exactamente con el dato; esta es la única que no. **No hay código que lo arbitre todavía**: `Domain/Achievements/` tiene hoy el catálogo (`AchievementCatalog.swift`) y el enum de métricas (`AchievementMetric.swift`), y el evaluador no existe, así que ninguna de las dos lecturas está contradicha por lo construido y decidirla aquí sería inventar la respuesta. La regla de A-5 aplica: lo que está abierto se dice abierto, con su dueño.
- source_spec: `_bmad-output/implementation-artifacts/spec-a5-reconciliar-planificacion.md`
  summary: `Domain/Ports/MotionPort.swift:5-6` sigue diciendo *"Dos modos que **no se combinan** (AD-21)"*, la misma redacción que el chore A-5 corrigió en AD-21 del spine. Los dos modos **sí conviven** —la reconciliación consulta con el stream abierto, y así lo hacen la 1.5 y la 1.6—; lo que no ocurre es que sus resultados se **sumen**, porque ambos entran por el mismo camino y el store se queda con el **máximo acumulado visto** (`highestCumulativeSteps`). Destino: la historia o el chore que vuelva a tocar `MotionPort` o su adapter. **Aquí no se arregla**: la Intent congelada de A-5 dice "Nada de código. Este chore no toca `Domain/`, `WalkTracker/`, `Scripts/` ni tests".
  evidence: La retro del Epic 1 (hallazgo **R10**) nombraba **dos** sitios con esa redacción ambigua, y el chore A-5 solo podía arreglar uno —el del spine, que es documentación—. Este es el otro, y es el que lee quien implementa. Verificado contra el código: `SessionStore+Reconciliation.swift:19-21` ("Consulta el tramo, no el gap … el máximo de `highestCumulativeSteps` evita contar dos veces lo que el stream entregue después") y `SessionStore+StepCounting.swift:71-73` (`if sample.steps > highestCumulativeSteps`). Es una línea de documentación en un puerto, sin conducta asociada: cambiarla no mueve ningún test, y por eso cabe en cualquier historia que pase por ahí.
- source_spec: `_bmad-output/implementation-artifacts/spec-a5-reconciliar-planificacion.md`
  summary: Tres textos del propio código siguen contando **dos** colores propios donde hay **tres**, desde que B-2 añadió `ErrorMessage` (2026-09-20). Los tres sitios: (a) la cabecera de `WalkTracker/UI/Style/DesignTokens.swift` —*"Los **dos** colorsets de aquí viven en `Resources/Assets.xcassets/`"*—, que se contradice con su propio `enum Colors`, que declara tres; (b) `Scripts/check-project-shape.sh:497`, que describe la sección 12 como *"los **dos** colores propios"* cuando esa misma sección la reescribió B-2 para vetar los doce cromáticos del sistema; y (c) nada más — el resto de la documentación ya dice tres. Destino: la historia o el chore que vuelva a tocar los tokens o el gate. **Aquí no se arregla**: "nada de código".
  evidence: Verificado el 2026-09-20 contra el árbol: `WalkTracker/Resources/Assets.xcassets/` tiene **tres** colorsets (`AccentColor`, `EstimatedSteps`, `ErrorMessage`) y `WalkTrackerTests/UI/DesignTokensTests.swift` los enumera como tres (`@Test("Los tres colorsets viajan dentro de la app", arguments: ["AccentColor", "EstimatedSteps", "ErrorMessage"])`). Son comentarios, no conducta: ningún test ni gate los mira, que es exactamente por lo que envejecieron sin que nadie se enterara. `DEROGACIONES.md §4` es desde este chore la **fuente única** de la tabla de los tres colores, así que lo barato es que estos comentarios remitan ahí en vez de repetir el recuento.
- source_spec: `_bmad-output/implementation-artifacts/spec-a5-reconciliar-planificacion.md`
  summary: **Decisión pendiente, y es el único desfase que ve un usuario.** `WalkTracker/Resources/achievements.json:13` describe `early_bird` como *"Camina antes de las 7:00"*, pero su regla es `{ "metric": "startHourLocal", "threshold": [5, 7], "comparison": "between" }` y **`between` es inclusiva en los dos extremos**: la franja llega hasta las **07:59**. El texto que Paul lee en la pantalla de logros contradice la regla que desbloquea el logro, y por debajo: quien camine a las 07:30 lo desbloquea creyendo que no debía. (`night_walker`, *"Camina después de las 21:00"*, sí es fiel a `[21, 23]`.) **No se arregla aquí, y no solo por la regla de "nada de código":** el catálogo de logros está declarado **contenido congelado** —*"se reutilizan íntegros, sin cambios de contenido"*—, así que cambiar la `description` **choca con esa restricción** y necesita una **decisión**, no un parche. Las salidas son tres y ninguna es obvia: reescribir la descripción (rompe el contenido congelado), estrechar el `threshold` a `[5, 6]` (cambia la semántica del logro) o dejarlo y asumir el desajuste. **Decidido por Paul el 2026-09-20: se corrige el TEXTO, no la regla** — el logro sigue desbloqueándose igual y solo deja de mentir. **Y al intentar aplicarlo, `verify-domain.sh` se puso en rojo:** `achievements.json#early_bird: description 'Camina antes de las 8:00' ≠ referencia 'Camina antes de las 7:00'`. El contenido congelado de AD-5 **no es una convención, es una regla mecánica**: el catálogo se compara contra la referencia de la v3, que es la base del sustrato de equivalencia. Aplicar la decisión exige **declarar una divergencia deliberada de la v3**, por el mismo mecanismo con el que el gate ya lista como esperadas las divergencias de hora local — y eso toca la tabla de divergencias de AD-6, que este mismo chore acaba de documentar como "de cuatro filas a dos". El cambio se probó y se revirtió el 2026-09-20. Destino: la historia **3.2**, que estrena el `AchievementEngine`, o un chore propio.
  evidence: Contrastado el 2026-09-20 en el chore A-5 entre `achievements.md#27`, `ARCHITECTURE-SPINE.md` (AD-5, enmienda del esquema del 2026-09-12, que fija `between` inclusiva) y `WalkTracker/Resources/achievements.json:13-14`. El mismo chore corrigió `epics.md:467`, que daba las franjas como 05:00–07:00 / 21:00–23:00, a **05:00–07:59 / 21:00–23:59** — y al hacerlo quedó a la vista que el dato de producción y su texto no dicen lo mismo. No hay evaluador todavía (`Domain/Achievements/` tiene hoy el catálogo y el enum de métricas, no el motor), así que nada del código construido arbitra el caso y decidirlo aquí sería inventar la respuesta. Se registra en vez de arreglarse, que es la regla de A-5: lo que está abierto se dice abierto, con su dueño.
- source_spec: `_bmad-output/implementation-artifacts/spec-b5-b6-gates-honestos.md`
  summary: Límite conocido de la **sección 6** del gate tras B-5: el receptor se reconoce por su **tipo declarado en el mismo fichero** (`let settingsStore: SettingsStore`, `func f(_ settings: SettingsStore)`, `let s = SessionStore(…)`), y para lo que no se declara ahí —un store al que se llega **a través de otro objeto**, como `root.sessionStore.isReconciling`— sigue valiendo **solo el respaldo por nombre** (sufijo `store`/`Store`). Es decir: un receptor tipado se caza aunque se renombre, y uno encadenado únicamente si además se llama como se llama. Cerrarlo pide seguir el tipo entre líneas (`root` es un `CompositionRoot`, y de ahí sale el tipo de `sessionStore`), que es análisis de tipos y no una regla léxica. Destino: el chore que vuelva a tocar el gate, o el día en que haga falta de verdad.
  evidence: B-5 cerró el defecto que la retro del Epic 2 denunció (D9): la regla definía "del store" **por el nombre de la variable**, así que `settings.save()` en una vista salía verde y lo único que sostenía el invariante era un comentario en producción pidiendo no renombrarla. Verificado el 2026-09-20 con la mutación en el árbol real —renombrar `settingsStore` a `settings` en `SettingsView.swift` y llamar a `settings.save()`—: el gate anterior salía **verde** y el nuevo falla con fichero y línea. El respaldo por nombre se conserva **a propósito** para no perder los casos encadenados que la regla anterior sí cazaba (el arnés los cubre: `root.sessionStore.isReconciling = false` en `App/`), y se declara aquí en vez de anunciarse como cubierto.
- source_spec: `_bmad-output/implementation-artifacts/spec-b5-b6-gates-honestos.md`
  summary: Límite conocido del **inventario de suites** de `Scripts/verify-domain.sh` tras B-6: lo que se deriva del árbol son los `@Suite` **dentro de `SUITE_DIRS`**, y `SUITE_DIRS` (`Domain`, `Vectors`, `Scenarios`) sigue siendo una lista escrita a mano. Un directorio de tests de dominio **nuevo** —`WalkTrackerTests/DomainExtra/`, pongamos— no lo mira nadie y el gate sale verde sin haberlo visto. No es el mismo defecto que B-6 cerró (dentro del alcance ya no hay nada que recordar), pero es la misma forma un piso más arriba. Salidas posibles: derivar también el alcance —de `project.yml`, o de qué importa cada suite— o aceptar por escrito que el alcance es una decisión y se revisa al crear un directorio de tests. Destino: el siguiente chore de gates, junto a la entrada de la sección 4.
  evidence: El alcance **es** una decisión deliberada y documentada (AD-6, hallazgo B3 de la 1.1, reaplicado por la 2.3 al sacar `SettingsStoreStrideTests`), así que derivarlo automáticamente cambiaría qué ejecuta el gate, y la Intent congelada de B-5/B-6 lo prohíbe explícitamente ("El alcance de `verify-domain.sh` no cambia: sigue siendo dominio y vectores"). Se registra para que "el gate pasa" no se confunda con "no hay suites de dominio sin ejecutar". El caso hermano —un `@Suite` **anidado** dentro de otro tipo, que `-only-testing:` nombraría por su ruta y la derivación línea a línea no sabe construir— **no** queda aquí: ese el gate lo convierte en un fallo explícito, y su caso rojo está en `Scripts/verify-domain-tests.sh`.
- source_spec: `_bmad-output/implementation-artifacts/spec-b5-b6-gates-honestos.md`
  summary: Límite conocido de la **sección 12c** tras B-5: la regla comprueba que el target `type: application` fija `ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor` y que existe **algún** `AccentColor.colorset` en el árbol, pero **no** que ese colorset esté en un catálogo que pertenezca al target de la app. Un `AccentColor.colorset` dentro de un `Assets.xcassets` de otro target —o suelto en una carpeta que ningún target compila— dejaría la regla en verde con la app sin acento. Salida posible: cruzar la ruta del colorset con las `sources:` del target de la app, que el gate ya parsea. Destino: el chore que vuelva a tocar el gate.
  evidence: B-5 cerró los dos defectos que la retro denunció (D9): la key se buscaba sobre `project.yml` **entero** —moverla al target de la extensión salía verde con la app sin acento— y **no se comprobaba a qué apunta** —borrar el colorset también salía verde—. Los dos casos están verificados con mutación en el árbol real el 2026-09-20 y tienen su caso rojo en `Scripts/check-project-shape-tests.sh`. Lo que queda es el tercer escalón, que hoy no tiene forma de ocurrir por accidente: el árbol tiene **un** catálogo (`WalkTracker/Resources/Assets.xcassets`) y está dentro de `path: WalkTracker`.
- source_spec: `_bmad-output/implementation-artifacts/spec-b5-b6-gates-honestos.md`
  summary: **Optimizar el coste del gate de forma, medido y con regresión.** B-5 hizo que la sección 6 derive los receptores **por tipo declarado** en vez de por el nombre de la variable, y eso cuesta: por cada `.swift` de `UI/` y `App/` se lanzan dos `grep`, dos `sed` y un `sort`. Medido en limpio, sin nada más corriendo: el gate pasa de **14 s a 18 s** (+29%) y su arnés de **889 s con 189 casos a 1.464 s con 208** (+65% total, **+50% por caso**: 4,70 s → 7,04 s). El gate corre como `preBuildScript` de los **dos** targets, así que añade ~8 s a cada build; el arnés pasa de 15 a 24 minutos antes de cada commit. **No es un problema de correctitud** —los 208 casos pasan y las cuatro mutaciones fallan como deben—, es coste del bucle de desarrollo. La vía obvia: una sola pasada de `awk` sobre todos los ficheros en vez de cinco procesos por fichero. Destino: un chore propio, o la primera historia a la que el coste le moleste.
  evidence: Medido el 2026-09-21 con `git stash` para comparar contra `4c0f3a6`, en ejecuciones aisladas — una medición previa bajo carga dio 267 s y era falsa. El informe de implementación afirmaba "5,1 s → 4,8 s, sin regresión de tiempo de build", y no se sostiene.
- source_spec: `_bmad-output/implementation-artifacts/spec-b9-degradacion-y-atribucion.md`
  summary: **CERRADO el 2026-09-21 — Paul lo decidió el mismo día y no esperó a la 3.1.** Estaba registrado así: *la app nombra la licencia del clima, pero no enlaza a su texto*; CC BY 4.0 §3(a)(1)(C) pide *"indicate the Licensed Material is licensed under this Public License, and include the text of, or the URI or hyperlink to, this Public License"*, y Open-Meteo lo repite (*"provide a link to the licence"*). El pie de "Acerca de" hacía la primera mitad y el enlace a `https://creativecommons.org/licenses/by/4.0/` vivía **solo en `NOTICE`**, que no viaja dentro del `.app`. Se aplicó la salida que esta misma entrada describía: **una segunda entrada en `AboutSection.Item`** (`licenseText`, con `licenseTextURL`), pintada por `SettingsView` y fijada por `AboutSectionTests` con su ruta —`creativecommons.org` a secas, o la 3.0, rompe la suite—. Siguen siendo **dos** filas: la versión de la app y los ajustes de otras épicas no se adelantan. **No queda trabajo**; la entrada se conserva como rastro de que estuvo abierto y de por dónde se cerró.
  evidence: Quedaba fuera de B-9 por su bloque congelado, que acotaba "Acerca de" a la atribución ("Never: … ni otras licencias") y por su matriz, que declaraba **un** enlace en Ajustes. **Paul renegoció esa línea el 2026-09-21**: la decisión está anotada dentro del bloque congelado de `spec-b9-degradacion-y-atribucion.md` —única edición permitida ahí—, con su fila de matriz y su fila en el Spec Change Log, que es el mecanismo que la retro del Epic 2 echó en falta cuando algo congelado se resuelve en contra. El mapa de cumplimiento requisito por requisito está en `NOTICE` §4, y §5 **conserva** la descripción del hueco y la fecha en que se cerró, en vez de borrarla; AD-24 lo refleja igual. La cláusula sigue transcrita de `https://creativecommons.org/licenses/by/4.0/legalcode.txt` (descargado el 2026-09-21, SHA-256 `9ba9550a…`), y la URL de la fila nueva es la **canónica** que ese mismo §2 registra: no se tecleó de memoria.
- source_spec: `_bmad-output/implementation-artifacts/spec-b9-degradacion-y-atribucion.md`
  summary: **Los tres checks de la sección "Acerca de" que solo se ven renderizando** — que con Dynamic Type al máximo el texto no se recorte, que VoiceOver anuncie la fila **como enlace** y lea su etiqueta, y que el objetivo táctil mida 44 pt de verdad. La suite cubre lo que la sección **contiene** y a dónde **lleva** (`AboutSectionTests`), que es lo que la decisión D1 del 2026-09-20 permite sin renderizar; lo físico no. Destino: **verificación manual en el iPhone**, junto a los checks pendientes de la 2.1 (B-8) y de la 2.3, no un arnés futuro.
  evidence: Es el mismo hueco ya registrado para el resto de Ajustes por la 2.3 y por B-2 —"el cableado de la vista no lo cubre ningún test"— y no es nuevo de esta sección: quitarle a `aboutLabel` su `.frame(minHeight: LayoutMetrics.touchTargetMin)`, o cambiar el `Link` por un `Text` sin destino, deja toda la suite en verde. Lo que B-9 **sí** cerró con mutación es lo que importaba: `AboutSection.items = []` hace fallar dos tests, y convertir `items` en función del clima rompe la compilación de la suite.
- source_spec: `_bmad-output/implementation-artifacts/spec-b9-degradacion-y-atribucion.md`
  summary: **CERRADO el 2026-09-21 — es un registro, no trabajo pendiente: el dueño del "Acerca de" ya quedó escrito en los cinco sitios que se enumeran.** Estaba registrado así: **Dónde quedó registrado el dueño del "Acerca de"**, que es la mitad de B-9 que no es código. La obligación de AD-24 se perdió la primera vez porque `epics.md` no nombraba esa pantalla **ni una vez**: no es que se olvidara, es que no era de nadie (lección **L2**, segunda vuelta con este mismo tema). Queda escrito en cinco sitios, a propósito, porque cada uno lo lee alguien distinto: `NOTICE` (la cláusula y el mapa de cumplimiento), **AD-24** del spine, el bloque 🔒 de `epics.md` bajo la historia 2.3 —con las historias que van a tocar Ajustes nombradas: 3.1, 4.2 y Epic 5—, la nota del épico 3, y el doc comment de `SettingsView` y de `AboutSection`. **No queda trabajo**: la entrada existe como rastro para la próxima historia de Ajustes, que es exactamente el lector al que iba dirigida.
  evidence: El compilador de contexto de cada épica lee los documentos de planificación (decisión D5 del 2026-09-20), así que un dueño escrito solo en el código no llega a la historia siguiente, y uno escrito solo en el spine no llega al que abre `epics.md`. Ya se comprobó en el Epic 2: al recompilar su contexto salió una advertencia falsa sobre esta misma atribución. Por eso el registro es redundante a propósito.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-persistencia-sesiones.md`
  summary: **El umbral a partir del cual la escritura síncrona de `sessions.json` deja de valer: ~5.000 registros (≈1,5 MB).** Hoy `StoragePort` es síncrono para los cuatro ficheros y `HistoryStore` reescribe el historial **entero** en el hilo principal **una vez por caminata**, no por muestra. Un registro cerrado ocupa ~300 B de JSON (16 campos, con clima y frase), así que las ~1.500 caminatas de diez años a tres por semana son unos **450 KB**: serializar y escribir eso son milisegundos, y por debajo de ese tamaño la sincronía es lo correcto —cada transición queda guardada antes de la siguiente y no hay estado intermedio que reconciliar—. **Revisarlo por encima de ~5.000 registros (≈1,5 MB)**, que es cuando una reescritura completa empieza a acercarse a un frame de 16 ms en un dispositivo antiguo. Las salidas, por orden de coste: escribir solo al cerrar y no en cada borrado de la 5.4; partir el fichero por año; o sacar la escritura del hilo principal, que rompe la atomicidad simple de AD-9 y es la última opción. **Ningún gate mide esto**: es un número escrito, no una comprobación. Destino: la historia que saque la escritura de `sessions.json` del hilo principal; hoy sin epic asignado.
  evidence: La documentación del puerto justificaba la sincronía en que "los dos ficheros ocupan unos cientos de bytes" (`Domain/Ports/StoragePort.swift`), y con el historial eso deja de ser cierto. La 5.1 la mantiene síncrona con el número delante en vez de heredar la frase antigua; el doc comment del puerto ya cita esta entrada.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-persistencia-sesiones.md`
  summary: **La transacción de AD-17 cruza dos ficheros con dos dueños, y no hay atomicidad conjunta.** AD-17 exige que los logros se evalúen **dentro de la misma transacción que persiste la sesión**; AD-16 da a `sessions.json` (`HistoryStore`) y a `achievements.json` (`AchievementsStore`) dueños distintos; y la escritura atómica de AD-9 es **por fichero** (temporal + `rename`). Las dos escrituras no pueden ocurrir de una sola vez: un corte entre ellas deja una caminata guardada cuyos logros no se evaluaron, o unos logros desbloqueados por una caminata que no se guardó. **El problema se crea en la 5.1 y se manifiesta en la 3.2**, que es su dueña. Salidas a valorar entonces, ninguna decidida aquí: (a) evaluar los logros **antes** de escribir el historial y escribirlos **después**, de modo que el peor caso sea un logro no evaluado y nunca uno fantasma, y reevaluarlo en el arranque siguiente; (b) un registro de intención en uno de los dos ficheros; (c) juntar los dos ficheros, que rompe AD-16. El punto de escritura ya está preparado y señalado en el código (`SessionStore+History.swift`, comentario `↓ AD-17`), y `SessionRecord.countsForAchievements` ya sabe que una huérfana no dispara logros (AD-18). Destino: la historia 3.2, que es su dueña.
  evidence: La 5.1 abre el punto de escritura que la 3.2 va a compartir (`confirmFinish()` → `saveFinishedWalk`), así que la incompatibilidad entre AD-16, AD-17 y AD-9 queda fijada por esta historia aunque no la sufra. Se nombra ahora en vez de descubrirla al implementar la 3.2.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-persistencia-sesiones.md`
  summary: **Los 79 vectores de equivalencia que esperaban al historial NO se portan aquí, y su destino es el Epic 3.** `weeklyProgress` (15) y `checkStreak` (8) → **historia 3.1**; `evaluateAchievements` (51) y `checkTimeOfDay` (5) → **historia 3.2**. La 5.1 les da lo único que les faltaba del lado de los datos —un historial persistido con `{startedAt, distanceM}` por caminata— pero **no el calendario**: `weeklyProgress` y `checkStreak` cuentan semanas y rachas y AD-19 exige que lo hagan con el **mismo `AppCalendar` expuesto por `ClockPort`** (ISO-8601, lunes primero, zona del dispositivo) que el anillo de meta, y ese tipo **no existe todavía**: lo crea la 3.1. Portarlos aquí obligaría a inventar el calendario fuera de su historia, que es justo el reparto que A-5 vino a cerrar. `evaluateAchievements` necesita además el evaluador de logros, que es el cuerpo de la 3.2. **La línea de pendientes de AD-6 sigue igual** —`achievementCatalog (1), checkStreak (8), checkTimeOfDay (5), evaluateAchievements (51), weeklyProgress (15)`— y eso es lo esperado, no un descuido. Destino: las historias 3.1 (`weeklyProgress`, `checkStreak`) y 3.2 (`evaluateAchievements`, `checkTimeOfDay`).
  evidence: `Scripts/verify-domain.sh` verde tras la 5.1 con la misma línea de pendientes que antes (255 tests en 18 suites). `VectorHarness.swiftDomain` no gana ninguna función en esta historia. El reparto por historia está en `epic-5-context.md` ("la consumen la 3.1 … y la 3.2 …") y en `epic-3-context.md`.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-persistencia-sesiones.md`
  summary: **Converger los tres `ReadOutcome` en uno** (parte de **B-10**, vocabulario transversal). La 5.1 saca a `WalkTracker/Application/StoredFileReadOutcome.swift` el vocabulario que B-1 estrenó dentro de `SettingsStore` —`loaded` / `absent` / `unreadable(StorageError)` con `allowsWriting`— y lo usan `HistoryStore` y `AchievementsStore`; **`SettingsStore` conserva de momento su tipo anidado** `SettingsStore.ReadOutcome`, porque migrarlo toca sus tests y su suite y no es de esta historia. Son dos tipos con la misma forma y el mismo invariante: el candidato exacto a divergir en silencio. Destino: el chore B-10.
  evidence: B-10 ("unificar el vocabulario transversal: una forma de acotar, un nivel de log por clase de suceso, el enum de resultado y `MeasurementLog`") sigue **open** en `sprint-status.yaml`. Esta entrada le da un caso concreto y localizado.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-persistencia-sesiones.md`
  summary: **Lo que el aviso de historial ilegible y el de "no se pudo guardar" NO tienen probado: que se vean.** La decisión de que haya aviso está cubierta en la lógica —`HistoryStore.showsUnreadableNotice` y `SessionStore.finishedWalkNotPersisted` tienen sus tests, incluidos los de mutación— pero que `HomeView` pinte el primero y `SessionSummaryView` el segundo, con su color medido, su contraste AA, su lectura de VoiceOver y sin recortarse con Dynamic Type al máximo, **solo se ve renderizando**. Destino: **verificación manual en el iPhone**, con los checks pendientes de la 2.1 (B-8), la 2.3 y B-9; no un arnés futuro (A-4 está cerrado y XCUITest descartado).
  evidence: Es el mismo hueco ya registrado para Ajustes y para "Acerca de": quitar el `if historyStore.showsUnreadableNotice` de `HomeView` o el `if walk.notPersisted` de `SessionSummaryView` deja toda la suite en verde. La decisión D1 del 2026-09-20 parte estos casos en contrato probable (cubierto) y físico (manual).
- source_spec: `_bmad-output/implementation-artifacts/spec-a7-gate-leccion-specs.md`
  summary: **Límite conocido de la regla (b) de `check-spec-shape.sh`: comprueba que la línea `**Campos compartidos…` EXISTE, no lo que dice.** Una spec puede escribir `- **Campos compartidos**: los de siempre.` y salir verde. Cerrarlo pide juzgar contenido, que es justo lo que el A-7 decidió no hacer (el gate comprueba forma, no intención), así que la salida realista no es un gate mejor sino comprobar si la marca se está rellenando en serio. Destino: la retro del Epic 3, que es la primera con specs escritas bajo la regla.
  evidence: Verificado sobre el propio gate: el patrón es `^[[:space:]]*([-*+][[:space:]]+)?\*\*Campos compartidos` y no mira nada detrás del marcador. Está declarado en la cabecera del script (límite 1) y aquí, porque B-5 obligó a que lo que un gate no cubre deje de anunciarse Y quede registrado: las cuatro entradas de límite conocido de B-5/B-6 son el precedente.
- source_spec: `_bmad-output/implementation-artifacts/spec-a7-gate-leccion-specs.md`
  summary: **Límite conocido de la regla (a): un `Destino:` con cualquier cosa detrás pasa, aunque esa cosa no nombre a nadie.** El gate exige la forma canónica y un carácter no blanco, no un dueño real: `Destino: por decidir` saldría verde. Dos de las 52 entradas vivas están justamente así —la del límite de la sección 12 (`hoy sin dueño`) y la del umbral de escritura síncrona (`hoy sin epic asignado`)—, y se dejan escritas con esa verdad en vez de inventarles un epic. Destino: la retro del Epic 3, con el límite hermano de la regla (b).
  evidence: Medido el 2026-09-21 sobre el fichero tras el arreglo del A-7: 52 entradas vivas con `Destino:`, de las cuales 2 declaran explícitamente que no tienen dueño todavía. Un gate que las rechazara empujaría a escribir un destino falso, que es peor que un destino honestamente vacío: el defecto que B-5 cerró era exactamente ése, reglas que obligan a maquillar el árbol para pasar.
- source_spec: `_bmad-output/implementation-artifacts/spec-a7-gate-leccion-specs.md`
  summary: **Nada comprueba que `check-spec-shape.sh` llegue a ejecutarse.** Lo invoca `workflow.on_complete` desde `_bmad/custom/bmad-build.toml`; si una actualización de la skill renombra ese escalar, el override del proyecto se vuelve un no-op silencioso, el gate deja de correr y ningún rojo lo dice. Es el defecto de B-6 —un mecanismo que depende de que alguien se acuerde— un piso más arriba: el CHECK es mecánico, la EJECUCIÓN sigue siendo un prompt. Destino: el siguiente chore de gates de la familia A-6 / B-5 / B-6.
  evidence: Comprobable hoy a mano y no automatizado: `uv run --no-cache _bmad/scripts/render_skill.py --project-root . --skill .claude/skills/bmad-build` y buscar el nombre del script en el `## On Complete` de `step-05-present.md` y de `step-oneshot.md` renderizados. El escalar lo declara `.claude/skills/bmad-build/customize.toml:32`, un fichero que la propia skill marca `DO NOT EDIT -- overwritten on every update`, así que el renombrado es un escenario real, no teórico.
