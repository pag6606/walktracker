---
epic: 1
date: 2026-09-14
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospectiva — Epic 1: Sesión y conteo de pasos nativo

## Epic summary

- **Epic:** 1 — Sesión y conteo de pasos nativo (`_bmad-output/planning-artifacts/epics.md`, historias 1.1–1.6;
  contexto compilado en `_bmad-output/implementation-artifacts/epic-1-context.md`).
- **Rango del diff:** `08a006914810b4c4f7895a78f7519623a5534f07..c3f1c65`. `08a0069` es el padre de `c9b9dd1`, el
  primer commit de la 1.1, y `c3f1c65` es el merge del PR #16. Son 18 commits: 12 de historia y 6 merges, los 6
  medidos.
- **Historias:** las 6 en `done` según `sprint-status.yaml`; `detect-epic --epic 1` da `pending_stories: []`.
  Cada historia tiene dos commits, la implementación y la verificación en el iPhone 14, y entró por PR:

  | Historia | Commits | PR |
  |---|---|---|
  | 1.1 | `c9b9dd1` + `ae8c4bd` | #11 |
  | 1.2 | `5099d34` + `f49ed61` | #12 |
  | 1.3 | `838fd4d` + `77eab2e` | #13 |
  | 1.4 | `5729a16` + `9cc991f` | #14 |
  | 1.5 | `7c246e0` + `c05dced` | #15 |
  | 1.6 | `e146a60` + `97a7797` | #16 |

- **Inventario de evidencia:**
  - **Disponible:**
    - Criterios de aceptación declarados por historia en `epics.md`.
    - Las 6 specs (`spec-1-1` a `spec-1-6`), con Implementation Notes, Review Triage Log y la verificación en el
      dispositivo.
    - `deferred-work.md` (9 entradas del epic).
    - `sprint-status.yaml`.
    - El historial git del rango.
    - Arquitectura: `ARCHITECTURE-SPINE.md` de 2026-09-12 y `DEROGACIONES.md`.
    - El log de sesión de las historias 1.5 y 1.6: esta conversación.
  - **Ausente:**
    - Retrospectiva previa: no hay ninguna `*retro*` en `_bmad-output`, y `sprint-status.yaml` no tiene clave
      `action_items`.
    - Los logs de sesión de las historias 1.1–1.4. Las lecciones de proceso de esas historias salen solo de sus
      specs y commits.

## Findings

**Cómo se obtuvieron:**
- **Cinco análisis en paralelo**, cada uno en un subagente:
  - la conciliación spec ↔ código;
  - arquitectura, tamaño, duplicación y patrones;
  - las lentes `adversarial`, `edge-case-hunter` y `verification-gap` de `bmad-review` sobre el diff de código del
    rango (47 ficheros, +7.397 líneas, sin `_bmad-output` ni `Localizable.xcstrings`).
- **Verificación en fuente primaria:** antes de enrutar un hallazgo lo abrí en el fichero, el commit o la spec. Los
  que no se sostuvieron se descartaron.
- **Hallazgos arrastrados:** los que ya se juzgaron en la revisión de una historia se marcan **(arrastrado)** con su
  veredicto original, y no se reabren.
- **Disposición de cada hallazgo:** *arreglar ahora* (pasa a action item), *diferir* o *aceptar*.

### Vistas agregadas

| # | Hallazgo | Fuente | Disposición |
|---|---|---|---|
| A1 | **`SessionStore` es ya una clase dios.** Tiene 750 líneas y 12 responsabilidades: intenciones de sesión, escena, permiso, stream, restauración, persistencia, reconciliación y `FirstResult`, entre otras. Cada historia le añadió entre 100 y 225 líneas (58 → 212 → 230 → 330 → 540 → 750). La MARK "Intenciones" mezcla sesión, escena y permiso, y `leaveSummary()` resetea a mano campos añadidos por la 1.4, la 1.5 y la 1.6. Los epics 3, 4, 5 y 7 van a colgar más responsabilidades de aquí. | `WalkTracker/Application/SessionStore.swift:38-750`; `git diff --numstat` de `c9b9dd1`…`e146a60`; `:308-320` | Arreglar ahora (A-1) |
| A2 | **`SessionStoreTests.swift` tiene 2.039 líneas y 103 tests.** Hay 6 suites, cada una con su propio montaje: 8 construcciones de `SessionStore`, `t0` repetido 5 veces y `startWalking` duplicado. Conviven tres estrategias de espera (`waitUntil` ×32, `stepCounting?.value` ×6 y dos `sleep` fijos de 20 ms). | `WalkTrackerTests/Application/SessionStoreTests.swift:16,46,110,146,454,599,967,1514`; `:605` = `:973`; `:847,1085` | Arreglar ahora, con A1 (A-1) |
| A3 | **Reglas de arquitectura que solo se cumplen por convención.** `check-project-shape.sh` solo comprueba imports en `Domain/` y `WalkTrackerActivity/`. Que solo `MotionAdapter` conozca CoreMotion, que el dominio no llame a `Date()`, que solo `SessionStore` escriba el snapshot (AD-16) y que ninguna vista importe un framework del sistema (AD-10) no tiene gate. Hoy se cumplen, salvo `MotionBlockedView`, que importa `UIKit` para `openSettingsURLString`. | `Scripts/check-project-shape.sh` (secciones 4 y 5); `WalkTracker/UI/Permission/MotionBlockedView.swift:2,56`; `CompositionRoot.swift:25` expone `storage` | Diferir (A-6); UIKit en la vista: aceptar con nota o mover al adapter (Q-4) |
| A4 | **Tres puntos de entrada para las fases de la escena, con filtros distintos.** `HomeView` (1.2) filtra en la vista con `@State`, `RootView` (1.5) delega el filtro al store y `WalkTrackerApp` (1.6) lanza la restauración con `.task`. El comentario de `RootView` dice ser "quien pasa las fases al store". | `WalkTracker/UI/Home/HomeView.swift:15-37`; `WalkTracker/UI/RootView.swift:11,62-74`; `WalkTracker/App/WalkTrackerApp.swift:14` | Arreglar ahora, con A1 (A-1) |
| A5 | **Constantes en el código a pesar de "se consolidan en `formulas.json`":** `autosaveIntervalS = 10`, `queryableHistoryS` (7 días) y `minPriorSampleS = 120`. Solo `minPaceDistanceM` justifica en su comentario por qué se queda en el código. | `Domain/Formulas/Formulas.swift:12-13`; `SessionStore.swift:136,583`; `Domain/Session/GapEstimator.swift:19`; `MetricsCalculator.swift:25-27` | Aceptar si se registra el criterio (A-5) |
| A6 | **Divergencias de convención que se acumulan entre historias:** <ul><li>los escenarios de la 1.5 y la 1.6 abandonan el prefijo `Nativo ·` que usan la 1.1–1.4 (0 frente a 2/3/9/10);</li><li>`log.fault` para "inalcanzable" solo en la 1.6, donde antes se usaba `error`;</li><li>líneas `///` de más de 90 columnas, que crecen por historia: 6 → 3 → 13 → 23 → 45 → 55;</li><li>la familia `confirmFinish` pasa a `async` en la 1.5 y `confirmDiscardEstimated` sigue síncrona;</li><li>el timeout en tiempo real (`Task.sleep`) queda fuera de `ClockPort`.</li></ul> | `grep -c 'Nativo ·' WalkTrackerTests/Scenarios/*`; `SessionStore.swift:462,465,486,531` frente a `:685`; métrica `awk length` por commit; `SessionStore.swift:226,263,652` | Prefijo: arreglar ahora (A-2). Resto: aceptar como lección (L-3) |
| A7 | **Duplicación menor.** `MetricsCalculator` reimplementa en línea la regla de la zancada. Hay cuatro formas de tratar el desbordamiento al redondear. La validación de rangos del snapshot está partida entre `Session.restore` y el store. | `MetricsCalculator.swift:38` frente a `Session.swift:117-123`; `Session.swift:286-290`, `MetricsFormat.swift:73-77`, `MetricsCalculator.swift:55-56`, `GapEstimator.swift:37-38`; `SessionStore.swift:471-474` | Aceptar (inofensivo hoy) |

### Conciliación spec ↔ implementación

Criterios de `epics.md` 1.1–1.6 frente a HEAD. Resumen de la conciliación:
- **Cumplidos con test:** la gran mayoría.
- **Cumplidos por derogación registrada:** AR-10, UX-DR2 y el puerto `query`.
- **Cumplidos por decisión de Paul:** rejilla 2×2, resumen mínimo, banner en la 1.5, 6 h y "Caminata recuperada".

Estas son las divergencias **sin decisión registrada**:

| # | Divergencia | Fuente | Disposición |
|---|---|---|---|
| S1 | **Cadencia sobre tiempo neto, no sobre "minutos con sensor activo".** La 1.3 lo dejó "hasta la 1.5", pero el Code Map de la 1.5 dice que `metrics(at:)` "No cambia". Tras un gap **estimado**, el tiempo cuenta y los pasos no, así que la cadencia sale a la baja. El traspaso no llegó a `deferred-work.md`. | `Domain/Session/Session.swift:315`; `spec-1-3-metricas-en-vivo.md:211`; `spec-1-5-reconstruccion-background.md:79` | Diferir; decisión de dominio (Q-2) |
| S2 | **Descartar pone a 0 todos los estimados**, no "los de ese gap" como dice `epics.md`. | `Session.swift:174-179`; `epics.md` Story 1.5 AC 4 | Aceptar y reconciliar la redacción (A-5) |
| S3 | **"Sesión recuperada" solo al relanzar**, frente al "force-quit o background" de `epics.md` y del contexto. Está en las Boundaries congeladas de la 1.6, que Paul aprobó, citando EXPERIENCE.md #95 frente a #84. | `spec-1-6-recuperacion-foreground.md:39-40`; `epics.md` Story 1.6 AC 1 | Aceptar y reconciliar la redacción (A-5) |
| S4 | **Autosave por muestras (como mucho cada 10 s)** frente al "cada 10 s" de AD-9 y del contexto. Está en las Boundaries congeladas de la 1.6 y lo justifica AD-21. | `SessionStore.swift:136,697`; `ARCHITECTURE-SPINE.md` AD-9 | Aceptar y reconciliar AD-9 (A-5) |
| S5 | **`WakeLockPort` no existe.** `epics.md` dice que "sobrevive como `WakeLockPort` (AD-10) en Epic 1", pero ninguna historia lo asumió y la 1.6 lo excluye explícitamente. | `epics.md:896`; `Domain/Ports/` (sin él); `spec-1-6` Never | Decisión (Q-3) |
| S6 | **No existe `DegradationPolicy` (AD-11), y `.unavailable` bloquea igual que el permiso denegado.** | `Domain/Ports/PermissionStatus.swift:7` (solo en un comentario); SST:218 | Aceptar; reconciliar AD-11 cuando llegue la primera degradación no bloqueante (clima, Epic 2) |
| S7 | **`SessionStatus` no tiene `idle`:** esa fase es `session == nil`. | `Domain/Session/SessionStatus.swift:8-11`; AD-8 | Aceptar y reconciliar AD-8 (A-5) |
| S8 | **`StoragePort` tiene un 4.º método (`setAsideActiveSession`) y el snapshot guarda el tramo.** Solo consta como desviación en las Implementation Notes de la 1.6. | `Domain/Ports/StoragePort.swift:36`; `Domain/Ports/ActiveSessionSnapshot.swift` | Aceptar; la 5.1 debe partir de aquí (A-5) |
| S9 | **`startedAt` no se materializa en ISO-8601:** el único formato persistido va en ms (§8). | `Session.swift:19`; `ActiveSessionFileAdapter.swift` | Diferir a la 5.1 (`sessions.json`) |
| S10 | **La regla de sesión huérfana (AD-18) solo corre al arrancar.** Si iOS suspende la app sin matarla y Paul vuelve 2 días después, `appDidBecomeActive()` reconcilia y estima 48 h de gap. Con un force-quit en el mismo tiempo de reloj, la sesión se cerraría. AD-18 dice literalmente "al arrancar". | `SessionStore.swift:291-298` frente a `:496`; `ARCHITECTURE-SPINE.md` AD-18 | Decisión (Q-1) |

### Revisión del diff del epic (lentes de código)

Se juzgaron 43 hallazgos brutos (adversarial 20, edge-case 19, verification-gap 4), consolidados por causa raíz:

| # | Hallazgo | Veredicto | Fuente | Disposición |
|---|---|---|---|---|
| R1 | **Un resultado de consulta menor que lo ya visto se trata como "sin dato" y estima.** Si CoreMotion da totales por rango algo inferiores al acumulado en vivo, Paul quieto 30 min en background recibiría unos 3.000 pasos estimados a su cadencia de caminar. El propio store anticipa acumulados no monótonos ("350 → 340 → 360"). | maybe-false; si es real, **high** | `SessionStore.swift:612`; `:117`; test `queryBelowSeenIsNoData` (SST:1138) | Medirlo en la 8.4 (A-3) y revisar la regla (Q-5) |
| R2 | **Doble cuenta tras una reconciliación degradada.** Con la consulta nil o agotada se estima, y el stream entrega **después** el acumulado del gap. La 1.6 lo extiende a la restauración: el stream se reabre desde `segmentStart` y se estima desde `savedAt`. El parche de la 1.5 solo cubre las muestras que llegan **durante** la consulta. | maybe-false, medium (arrastrado de la 1.5 y ampliado) | `SessionStore.swift:612-630`, `:507-512`; `deferred-work.md` (1.5) | Medirlo en la 8.4 (A-3) |
| R3 | **La estimación no tiene tope.** Un gap de 6 h tras restaurar, o de días al volver de una suspensión (S10), se multiplica por la cadencia. | low en la 1.5 (arrastrado); la 1.6 y S10 lo agravan | `Domain/Session/GapEstimator.swift:48-60`; `SessionStore.swift:603-607` | Con Q-1 |
| R4 | **El parche de la 1.6 sobre `lastSampleAt` está incompleto.** La consulta ya no mueve `lastSampleAt`, pero la primera muestra del stream tras volver o restaurar es acumulada y trae `end ≈ ahora`: sí lo mueve, con lo que una huérfana posterior se recorta en el momento de volver. | medium (real al leer el código) | `SessionStore.swift:681` | Arreglar ahora (A-2) |
| R5 | **Si el sistema termina el stream, nadie lo reabre ni lo muestra.** `isCountingSteps` pasa a `false`, pero ninguna vista lo lee y nada lo reabre al volver a primer plano: los pasos se congelan en silencio. | medium | `SessionStore.swift:705-709`; `grep isCountingSteps WalkTracker/UI` = 0 | Diferir; decisión (Q-6) |
| R6 | **Al pausar se pierden los pasos atrasados.** La 1.4 lo rechazó porque "recuperarlos exige la consulta de la 1.5", y la 1.5 solo reconcilia al finalizar. El traspaso se perdió. | low (arrastrado de la 1.4) | `SessionStore.swift:172-185`; `spec-1-4` Triage #2 | Aceptar; lección L-2 |
| R7 | **`StorageStub` aparta el snapshot ante cualquier error de lectura, también `.failed`.** El adapter no lo aparta con `.failed`, así que los tests comprueban una semántica distinta de la de producción. | low (real) | `WalkTrackerTests/Support/StorageStub.swift:31-42` frente a `ActiveSessionFileAdapter.swift:308-323` | Arreglar ahora (A-2) |
| R8 | **Apartar un snapshot machaca el apartado anterior.** `rename(2)` sobre el nombre fijo `activeSession.corrupt.json` destruye el primero, en contra de la Boundary "se aparta, no se destruye" de la 1.6. | low (real) | `ActiveSessionFileAdapter.swift:93-97`; `spec-1-6` Never | Arreglar ahora (A-2) |
| R9 | **`MotionStub` no reproduce la semántica del adapter:** no usa `.bufferingNewest(1)` ni termina el stream anterior. Los tests de muestras rancias y de reemplazo de stream no ejercitan lo que ocurre en el dispositivo. | low (documentado a propósito en el stub) | `WalkTrackerTests/Support/MotionStub.swift:8-10` | Aceptar con nota |
| R10 | **El contrato del puerto dice que las actualizaciones y la consulta "no se combinan" (AD-21)**, pero la 1.5 y la 1.6 consultan con el stream abierto. La intención era no **sumar** las dos fuentes, y la redacción da pie a otra lectura. | low | `Domain/Ports/MotionPort.swift:5-6`; `ARCHITECTURE-SPINE.md:266` | Reconciliar la redacción (A-5) |
| R11 | **Distancia entre tramos con fuentes mezcladas** (base 0 si un tramo previo no tuvo distancia del sistema). | maybe-false (arrastrado de la 1.3 y la 1.4, ya diferido) | `SessionStore.swift:419`; `deferred-work.md` | Medirlo en la 8.4 (A-3) |
| R12 | **Ya triados como low en la 1.5 y la 1.6 (arrastrados), sin cambios:** <ul><li>volver a background durante la reconciliación;</li><li>botón "Iniciar" muerto mientras restaura;</li><li>permiso revocado entre el force-quit y el relanzamiento;</li><li>validación cruzada de los instantes del snapshot;</li><li>reintento de una lectura fallida;</li><li>fallo al borrar el snapshot;</li><li>timeout enorme;</li><li>`confirmFinish` descartado si coincide con una reconciliación;</li><li>`TimelineView` repintando en pausa;</li><li>autosave con el reloj hacia atrás;</li><li>inicio del gap anterior a la última muestra.</li></ul> | low | Review Triage Logs de `spec-1-5` (#4, #7) y `spec-1-6` (#3–#6, #8, #13, #19) | Aceptar |

**Huecos de verificación entre historias** (pre-verificados por la lente; ninguna suite de una sola historia los ejerce):

| # | Hueco | Fuente | Disposición |
|---|---|---|---|
| V1 | Rama de distancia **del sistema más estimados**, la del iPhone real, donde CoreMotion da distancia: ningún test combina `systemDistanceM` con `stepsEstimated > 0`. Quitar el término de estimados dejaría el banner y "Descartar" sin efecto en la distancia, y todo seguiría en verde. | `Session.swift:306-308`; `MetricsScenarios:118-127`; `GapReconstructionScenarios:42-48,108-124` | Arreglar ahora (A-2) |
| V2 | Resumen de la huérfana: ningún test afirma `store.metrics` tras `closeOrphan`. Sin esa asignación, la cubierta se quedaría en blanco y sin salida al arrancar. | `SessionStore.swift:536`; `SessionView.swift:63-65`; tests de huérfana en SST:1691-1780 | Arreglar ahora (A-2) |
| V3 | Campos del tramo que escribe el store (`segmentSteps`, `distanceBaseM`) tras pausar y reanudar: todos los tests de restauración usan snapshots hechos a mano o de un solo tramo. | `SessionStore.swift:559-560`; SST:1587, 1946 | Arreglar ahora (A-2) |
| V4 | `backgroundedAt = nil` después de restaurar: ningún test hace un ciclo background → activo tras restaurar sin dato. | `SessionStore.swift:513`; SST:2018-2021 | Arreglar ahora (A-2) |
| V5 | Sin arnés de tests de vista. Es el mismo hueco diferido en **5 de las 6 historias** (1.1, 1.2, 1.4, 1.5, 1.6). | `deferred-work.md` | Decisión de proceso (A-4) |

### Lecciones de proceso

Salen de las specs, los commits y el log de sesión de la 1.5 y la 1.6. De la 1.1–1.4 no hay log.

| # | Lección | Fuente |
|---|---|---|
| L1 | **Las 6 specs superaron el tamaño recomendado y en las 6 se mantuvieron completas.** La norma de ≤ 1.600 tokens no encaja con historias que cruzan dominio, store y UI en este proyecto. O se ajusta el umbral, o se trocea antes, en la creación de epics. | `grep "La spec se mantiene completa" spec-1-*.md` = 6/6 |
| L2 | **Los traspasos entre historias se pierden cuando viven solo en Design Notes o en el Triage Log.** Tres casos: <ul><li>la cadencia de la 1.3 "hasta la 1.5" (S1);</li><li>los pasos al pausar, de la 1.4 a la 1.5 (R6);</li><li>`WakeLockPort` "en Epic 1" (S5).</li></ul> Ninguno llegó a `deferred-work.md`, que es lo único que la historia siguiente lee. | `spec-1-3:211`; `spec-1-4` Triage #2; `epics.md:896`; `deferred-work.md` |
| L3 | **Cada historia que toca `SessionStore` añade estado compartido sin revisar lo que ya había.** Las revisiones de historia encontraron entre 5 y 8 parches cada una, varios de lógica entre historias (1.5: doble cuenta durante la consulta; 1.6: recorte de la huérfana). Aun así, la retro encuentra un parche incompleto (R4). Al Code Map de una spec que toca el store le falta la lista de campos compartidos y sus invariantes. | Triage Logs de `spec-1-5` (#2, #7) y `spec-1-6` (#1, #2); R4 |
| L4 | **Medir en el iPhone.** Cuatro dudas de comportamiento de CoreMotion llevan varias historias diferidas "hasta registrar muestras en el iPhone 14": alternancia de la distancia, fuentes mezcladas entre tramos, doble cuenta tras una consulta nil y consulta menor que lo visto. Ninguna historia instrumentó esa medición. | `deferred-work.md` (1.3, 1.4, 1.5); R1 |
| L5 | **El estado de un merge se dio por hecho sin comprobarlo:** Paul dijo que el PR #15 estaba mergeado y seguía `OPEN`. Se detectó al comprobarlo con `gh` antes de sacar la rama de la 1.6. | Log de sesión (2026-09-14), `gh pr view 15` |

## Behavior verification

- **Automatizada, sobre `main` (`c3f1c65`), en esta retro:**
  - `bash Scripts/verify-domain.sh` en verde: inventario de AD-6, camino rojo del arnés y 157 tests en 11 suites.
  - `xcodegen generate && xcodebuild test … iPhone 16e`: `TEST SUCCEEDED`, 316 tests en 30 suites, sin warnings propios.
- **En el dispositivo, registrada por historia.** Paul la hizo en el iPhone 14 sobre el build de cada historia; consta
  en la sección de Implementation Notes de cada spec:
  - **1.1:** cronómetro y cubierta.
  - **1.2:** conteo con pantalla bloqueada y música, precisión frente a Salud.
  - **1.3:** métricas y tamaños de texto.
  - **1.4:** pausar, reanudar, finalizar y VoiceOver.
  - **1.5:** pasos al instante tras bloquear 5 min, finalizar con los últimos pasos, y pausa con bloqueo.
  - **1.6:** force-quit con la sesión activa, pausada y finalizada.
- **No se ejercitó en esta retro:** el comportamiento en el dispositivo sobre el `main` integrado; desde esta sesión no
  se puede manejar el iPhone. Tampoco los flujos entre historias que solo existen juntos: restaurar y después pausar
  o descartar, una huérfana con estimados, o una sesión larga con varias salidas a background. Son los que mide la
  8.4 (caminata de 30 min con el build de TestFlight).

## Previous-retro follow-through

No hay nada que revisar. No existe retrospectiva previa del proyecto: no hay ningún documento `*retro*` en
`_bmad-output`, y `sprint-status.yaml` es legible pero no tiene la clave `action_items`.

## Action items

Son **propuestas**: la retro no aplica código ni edita la spec del proyecto, y Paul decide qué se ejecuta y cuándo.
- **Ruta:** los arreglos de código pasan por el ciclo normal (`bmad-build`), y las reconciliaciones de spec por Paul o
  por `bmad-correct-course`.
- **Owners:** *Paul* decide o reconcilia; *Dev* es una historia o chore con `bmad-build`; *8.4* es trabajo de la
  historia del gate.

| ID | Acción | Owner | Tipo | Hallazgos |
|---|---|---|---|---|
| A-1 | **Refactor antes del Epic 2** (sin cambiar comportamiento). <ul><li>Partir `SessionStore` por sus fronteras naturales: flujo de inicio y permiso; recuperación y persistencia; reconciliación; `FirstResult` a su fichero.</li><li>Centralizar el reset de estado de sesión que hoy repite `leaveSummary()`.</li><li>Unificar la entrada de fases de escena en un solo punto.</li><li>Partir `SessionStoreTests.swift` en un fichero por suite, con un soporte común (`Fixture`, `t0`, `startWalking`) y sin `sleep` fijos.</li></ul> | Dev | Remediación | A1, A2, A4 |
| A-2 | **Chore de correcciones y tests entre historias.** <ul><li>Que la primera muestra del stream tras volver o restaurar no mueva `lastSampleAt` más allá del inicio del gap (R4).</li><li>`StorageStub` sin apartar con `.failed` (R7).</li><li>Nombre único para el snapshot apartado (R8).</li><li>Tests de V1–V4.</li><li>Recuperar el prefijo `Nativo ·` en los escenarios de la 1.5 y la 1.6.</li></ul> | Dev | Remediación | R4, R7, R8, V1–V4, A6 |
| A-3 | **Instrumentar la caminata de la 8.4** para zanjar las dudas de CoreMotion que llevan varias historias diferidas. Registrar (`OSLog`, exportable): <ul><li>cada muestra del stream (`steps`, `distance`, `end`);</li><li>cada consulta de reconciliación (rango, resultado, `seen`, duración);</li><li>cada estimación.</li></ul> Con esos datos, decidir R1 (consulta < visto), R2 (doble cuenta tras degradar), R11 y la alternancia de distancia (1.3), además del timeout y el umbral de huérfana medidos. | 8.4 | Medición | R1, R2, R11, L4 |
| A-4 | **Decidir un arnés de verificación de vistas.** Por ejemplo, un target de UI tests con XCUITest (diferido en el spine) o extraer la lógica de presentación a tipos probables (`FinishedWalk(session:metrics:)`, mapeo fase → intención). Cierra los 6 diferidos de vista del epic. | Paul | Proceso | V5 |
| A-5 | **Reconciliar la spec del proyecto con lo construido y aceptado:** <ul><li>`epics.md` 1.5 AC 4: descarte global (S2);</li><li>`epics.md` 1.6 AC 1: aviso solo al relanzar (S3);</li><li>AD-9: autosave por muestras (S4);</li><li>AD-8: sin `idle` en `SessionStatus` (S7);</li><li>AD-21 y `MotionPort`: "no se combinan" significa no sumar fuentes (R10);</li><li>`StoragePort` y el snapshot con el tramo como punto de partida de la 5.1 (S8);</li><li>criterio sobre qué constantes van a `formulas.json`: calibraciones medibles sí, hechos del sistema y reglas de producto no (A5).</li></ul> | Paul | Reconciliación de spec | S2–S4, S7, S8, R10, A5 |
| A-6 | **Extender `check-project-shape.sh`** con las reglas que hoy dependen de revisión: <ul><li>CoreMotion solo en `Adapters/Motion`;</li><li>nada de `Date()` ni `Date.now` en `Domain/`;</li><li>llamadas a `StoragePort` solo desde `SessionStore`;</li><li>ningún framework de sistema en `UI/`.</li></ul> | Dev | Remediación | A3 |
| A-7 | **Lección para las specs** (L2, L3): <ul><li>todo traspaso "hasta la historia X" se registra en `deferred-work.md` con esa historia como destino, nunca solo en Design Notes o en el Triage Log;</li><li>una spec que toque estado compartido de `SessionStore` lista en su Code Map los campos que ya existen y sus invariantes.</li></ul> | Paul | Proceso | L2, L3 |
| A-8 | **Revisar el umbral de tamaño de spec para este proyecto**, o trocear las historias al crear los epics: 6 de 6 lo superaron y se mantuvieron completas (L1). | Paul | Proceso | L1 |

## Acceptance verdict

**accepted-with-open-items** (veredicto de máquina; criterios **declarados**, los AC de `epics.md` 1.1–1.6).

- **Historias:** las 6 están en `done`; `pending_stories` está vacío.
- **Criterios cumplidos con evidencia:** cada AC está cumplido con test, por derogación registrada o por decisión de
  Paul, salvo S1–S10. Hay verificación en el iPhone 14 por historia, y la suite completa y `verify-domain.sh` están en
  verde sobre `main`.
- **Batería y precisión en 30 min:** el propio epic las asigna a la 8.4 (`epic-1-context.md`, Cross-Story Dependencies).
  No es un criterio incumplido de este epic.
- **Por qué no es `accepted`:**
  - R1 es un maybe-false con consecuencia potencialmente alta (pasos fantasma).
  - A1 (clase dios) y los huecos V1–V4 están abiertos.
  - Hay decisiones pendientes: S10, S5, S1 y R5.

  Ninguno es bloqueante para declarar el epic entregado, y todos quedan con action item o pregunta.
- **Decisión humana:** pendiente de confirmación de Paul. Un override suyo prevalece.

## Open questions

| ID | Pregunta | Qué cambia la respuesta | Hallazgos |
|---|---|---|---|
| Q-1 | ¿La regla de sesión huérfana y un tope de estimación deben aplicarse también al volver de una suspensión larga, no solo al arrancar? | Cambia AD-18 y el comportamiento de `appDidBecomeActive()`, y acota los pasos estimados por gaps de horas o días. | S10, R3 |
| Q-2 | ¿La cadencia debe excluir el tiempo de los gaps estimados ("minutos con sensor activo")? | Es un cambio de dominio con vectores nuevos. Hoy la cadencia baja tras un gap estimado. | S1 |
| Q-3 | ¿`WakeLockPort` sigue siendo necesario, ahora que el conteo funciona con la pantalla bloqueada? Si lo es, ¿en qué epic? | Hay que asignarlo a una historia o retirarlo de `epics.md`. | S5 |
| Q-4 | `MotionBlockedView` importa `UIKit` para abrir Ajustes: ¿se acepta como excepción de AD-10 o se mueve al adapter? | Cambia la regla del gate de A-6. | A3 |
| Q-5 | Si la 8.4 confirma que la consulta puede dar menos que lo visto, ¿cualquier respuesta no nil cuenta como "el sistema tiene el dato" y no se estima nunca? | Cambia la regla de la 1.5 y el test `queryBelowSeenIsNoData`. | R1 |
| Q-6 | Si el sistema termina el stream con la sesión viva, ¿se reabre solo al volver a primer plano, se muestra un aviso, o las dos cosas? | Cambia la decisión de la 1.2 ("la sesión sigue y conserva sus pasos"). | R5 |
